import { describe, it } from 'vitest';
// NOTE: import path is forward-declared — Wave 2 implements this component.
// This file is intentionally RED/pending. Wave 2 replaces it.todo with real assertions.

describe('DeckWorkspace', () => {
  it.todo('renders "Deck not found" when URL id has no matching deck (after loading=false)');
  it.todo('shows loading state while useDeckStore loading=true');
  it.todo('renders WorkspaceHeader, CommanderPanel, CardSearchSection stubs for a valid deck id');
  it.todo('"Back to decks" link has href="/"');
});
