---
context: bug-investigation
phase: post-03.1
status: blocked_on_user
last_updated: 2026-04-15T03:50:57.792Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Blind variation debugging without symptom | Stubbed repro came back clean; was about to spawn 7 variant specs (no-commander / multi-card / reload / direct-URL / toggle-view / etc.) chasing a bug with no known symptom. Context at 80%. | advisory | Ask user for exact console message before writing any more variant specs. Stubbed-clean + no symptom = the bug depends on real data the fixtures don't cover; guessing won't close the gap. |

<current_state>
Phase 03.1 is complete and verified. User reported a bug on the flow: create deck → open → choose commander → add card → back to list → open again → observe console. I built a Playwright repro spec (`e2e/specs/99-repro-second-open.spec.ts`) using the project's `stubScryfall` + `commanderFlows` + `deckBuildingFlows` helpers. The spec ran green with a **clean console on the second open** — no errors, warnings, or unexpected output. The bug therefore depends on something the stubs don't reproduce (real Scryfall response shape, untested card path, or a state transition the fixtures miss).

Blocked on the user sharing the exact console text they observed.
</current_state>

<completed_work>

**Phase 03.1 — shipped and committed (main @ 9a37b5a):**
- 03.1-01 CommanderPanel focus rings on action buttons (Flip / Change commander / Remove partner)
- 03.1-02 Scroll-reset ref lifted from DeckColumn to DeckWorkspace outer wrapper; `onViewToggle` prop wire
- 03.1-03 CardResultCell outer container focus ring with `tabIndex={0}` preserved
- 03.1-04 Playwright spec `14-ui-polish.spec.ts` — 3 browser-level regression tests (computed box-shadow + 2000px-spacer scroll reset)
- Code review: 0 critical, 2 warning (WR-01 cursor-pointer on non-clickable container; WR-02 DeckWorkspace "Deck not found" flash on first visit — both pre-existing), 4 info
- Verification: PASSED 5/5 must-haves
- Tests: unit 287/287 · e2e 30/30 · typecheck clean

**Bug investigation so far:**
- Wrote `e2e/specs/99-repro-second-open.spec.ts` using proper helpers (stubScryfall + createDeck + selectCommander + addCardToDeck)
- Ran the exact user-described flow; captured full console (all types, not just errors)
- Result: clean — only benign Vite HMR + React DevTools + Persistent storage messages (all on first open, none on second open)
</completed_work>

<remaining_work>

1. **Get symptom from user** (BLOCKING) — exact console message text + type, or a devtools-capture snippet output
2. **Target the symptom** once known:
   - If real-data-dependent → add fixture for the specific card
   - If state-transition bug → add the missing step to the repro spec
   - Likely suspects given 03 architecture: `resolveOriginalReleaseDate` pipeline (oracle_id search on add), Dexie hydration on second open, StrictMode double-effect, DeckWorkspace "Deck not found" flash (WR-02 from review)
3. **Decide on scratch file fate**:
   - Promote `99-repro-second-open.spec.ts` to real regression spec (rename, scope properly, keep after bug is fixed)
   - OR delete both scratch files (`_repro.mjs`, `99-repro-second-open.spec.ts`) after investigation
</remaining_work>

<decisions_made>

- **Use Playwright spec over standalone node script** — failed standalone attempt (`_repro.mjs`) fought with selector discovery; spec form leverages `commanderFlows.ts` and `deckBuildingFlows.ts` directly
- **Use stubScryfall over real network** — faster, deterministic, doesn't hit rate limits; acceptable that it doesn't reproduce (confirms bug is real-data-dependent)
- **Don't spawn variation specs without a symptom** — 80% context + no known message = shots in the dark
</decisions_made>

<blockers>
- User-provided console output required before continuing (see Anti-Patterns)
</blockers>

## Required Reading (in order)
1. `e2e/specs/99-repro-second-open.spec.ts` — the existing repro harness
2. `e2e/helpers/commanderFlows.ts` + `e2e/helpers/deckBuildingFlows.ts` — real selectors
3. `.planning/phases/03.1-ui-polish-phase-3-follow-ups/03.1-REVIEW.md` — WR-02 (DeckWorkspace "Deck not found" flash) is a prime suspect
4. `src/components/DeckWorkspace.tsx:15-46` — first-visit render logic flagged by WR-02
5. `src/store/deck-cards-store.ts` — Dexie load path on second open
6. `src/lib/scryfall.ts` + `resolveOriginalReleaseDate` — oracle_id prints query that fires on card add

## Infrastructure State

- Branch: `main` (HEAD: 9a37b5a)
- Working tree: 2 untracked scratch files (`_repro.mjs`, `e2e/specs/99-repro-second-open.spec.ts`) + pre-existing `.planning/.next-call-count`, `.planning/ui-reviews/`
- Dev server: not currently running
- Tests at pause: unit 287/287 ✅ · e2e 30/30 ✅ (the scratch 99-repro spec adds one more passing test if counted) · typecheck ✅

<context>
User ran /gsd-execute-phase 03.1; it completed cleanly (all 4 plans, verification 5/5, code review advisory only). Then they said "there is a bug i found" and walked through the flow. I asked for specifics; they said "Create deck, open deck, choose commander, add card to deck, go back to deck list, open deck again, observe console." I took that as the full spec and built a stubbed repro that came back clean. Then they said "try a few different variations on the test" — I was about to fan out into 7 variant specs when the context warning fired at 80%. I stopped, explained the stubbed-clean result means we need the actual symptom, and suggested the user paste the console text or use a devtools-capture snippet. They said "handoff".

The right resume move is NOT to guess more variations. It's to get the symptom. If the user can't easily share it, offer to start the dev server and watch together, or drive a headed Playwright session (`--headed`) so they can observe.
</context>

<next_action>
Read this file, read `e2e/specs/99-repro-second-open.spec.ts`, then ask the user for the exact console message text (or a screenshot). Do NOT write more variant specs before symptom is known.

If user wants to continue right now: offer `npx playwright test 99-repro-second-open --headed --workers=1` so they can observe the run, OR start dev server (`npm run dev -- --port 5179`) and walk the flow together with devtools open.
</next_action>
