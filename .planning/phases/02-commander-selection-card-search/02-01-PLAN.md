---
phase: 02-commander-selection-card-search
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/types/card.ts
  - src/lib/db.ts
  - src/lib/db.test.ts
  - src/lib/partner-detection.test.ts
  - src/lib/scryfall-client.test.ts
  - src/lib/card-cache.test.ts
  - src/store/commander-store.test.ts
  - src/store/card-search-store.test.ts
  - src/components/DeckWorkspace.test.tsx
autonomous: true
requirements:
  - CMDR-01
  - CMDR-02
  - CMDR-03
  - CMDR-04
  - CMDR-05
  - SRCH-01
  - SRCH-02
  - SRCH-03
  - SRCH-04
  - SRCH-05
  - SRCH-06
  - SRCH-07

must_haves:
  truths:
    - "react-router-dom@7.x is installed and present in package.json dependencies"
    - "Dexie database bumps to version 2 with an additional `cards` store keyed by `oracle_id` indexed on `cachedAt`"
    - "v1 decks, deckCards, deckChanges data survives the v2 migration"
    - "Wave 0 test files exist for every Wave 1/2 module (pending RED status)"
    - "`CachedCard` type is exported from src/types/card.ts"
  artifacts:
    - path: "src/types/card.ts"
      provides: "CachedCard type definition"
      contains: "export interface CachedCard"
    - path: "src/lib/db.ts"
      provides: "Dexie v2 schema with cards store"
      contains: "this.version(2).stores"
    - path: "src/lib/db.test.ts"
      provides: "v1 -> v2 migration test coverage"
      contains: "v1"
    - path: "src/lib/partner-detection.test.ts"
      provides: "Pending tests for CMDR-04/05"
    - path: "src/lib/scryfall-client.test.ts"
      provides: "Pending tests for SRCH-01..04, SRCH-06, CMDR-01"
    - path: "src/lib/card-cache.test.ts"
      provides: "Pending tests for read-through cache + TTL"
    - path: "src/store/commander-store.test.ts"
      provides: "Pending tests for CMDR-02, CMDR-03"
    - path: "src/store/card-search-store.test.ts"
      provides: "Pending tests for SRCH-05, SRCH-07"
    - path: "src/components/DeckWorkspace.test.tsx"
      provides: "Pending tests for routing + not-found"
  key_links:
    - from: "package.json"
      to: "react-router-dom@7"
      via: "dependencies entry"
      pattern: "\"react-router-dom\":"
    - from: "src/lib/db.ts"
      to: "cards store"
      via: "version(2).stores block"
      pattern: "cards: 'oracle_id, cachedAt'"
    - from: "src/lib/db.ts"
      to: "CachedCard type"
      via: "Table<CachedCard, string> declaration"
      pattern: "Table<CachedCard"
---

<objective>
Lay the Wave 0 foundation for Phase 2: install react-router-dom, bump Dexie to v2 with a new `cards` store, declare the `CachedCard` type, extend db migration tests, and create skeleton (RED) test files for every module that later waves will implement.

Purpose: Downstream waves require: (1) the router dep present, (2) the Dexie `cards` store schema already live so stores/libs can use it, (3) a complete set of failing/pending test files so each task in later waves has an `<automated>` verify target per Nyquist.

Output: A working build with Dexie v2, new type file, and 7 test files either added or extended (existing `db.test.ts` gets migration coverage; six new test files are committed as RED placeholders).
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-commander-selection-card-search/02-CONTEXT.md
@.planning/phases/02-commander-selection-card-search/02-RESEARCH.md
@.planning/phases/02-commander-selection-card-search/02-VALIDATION.md
@.planning/phases/01-foundation-deck-management/01-01-SUMMARY.md
@src/lib/db.ts
@src/lib/db.test.ts
@src/types/deck.ts
@src/test/setup.ts
@package.json

<interfaces>
<!-- Existing Dexie class - must extend without breaking v1 -->

From src/lib/db.ts:
```typescript
export class EDHBuilderDB extends Dexie {
  decks!: Table<Deck, number>;
  deckCards!: Table<DeckCard, number>;
  deckChanges!: Table<DeckChange, number>;

  constructor() {
    super('EDHBuilder');
    this.version(1).stores({
      decks: '++id, updatedAt',
      deckCards: '++id, deckId, scryfallId',
      deckChanges: '++id, deckId, timestamp',
    });
  }
}
export const db = new EDHBuilderDB();
```

From src/types/deck.ts (existing):
```typescript
export interface Deck {
  id?: number;
  name: string;
  commanderId: string | null;
  commanderName: string | null;
  colorIdentity: string[];
  createdAt: number;
  updatedAt: number;
}
```

New `CachedCard` shape (this plan adds it to src/types/card.ts):
```typescript
import type { ScryfallCard } from '@scryfall/api-types';

export interface CachedCard {
  oracle_id: string;         // primary key
  cardJson: ScryfallCard.Any;
  cachedAt: number;           // Date.now()
}
```

Scryfall API types are already present via `@scryfall/api-types@1.0.0-alpha.4`.
</interfaces>
</context>

<threat_model>
### Threats

| ID | Threat | Likelihood | Severity | Mitigation |
|----|--------|------------|----------|------------|
| T-02-01-A | Installing malicious/typosquatted `react-router-dom` | LOW | HIGH | Install the exact canonical name `react-router-dom` from npm registry; verify version 7.x before committing. Use `npm install react-router-dom` (not an alternative spelling). Confirm resolved version with `npm ls react-router-dom`. |
| T-02-01-B | Dexie migration drops v1 data | LOW | HIGH | Never modify the `version(1).stores(...)` block. Add a NEW `version(2).stores(...)` block that redeclares all v1 stores verbatim plus the new `cards` store. Write a migration test that seeds v1 data, opens v2, and asserts v1 decks still exist. |
| T-02-01-C | `cards` store key collision / data poisoning | LOW | MEDIUM | Primary key is `oracle_id` (Scryfall-assigned UUID). Do NOT use user-supplied values as keys. |

**ASVS L1 coverage:** V1.1 secure design (migration is additive), V14.2 dependency integrity (pinned dep install).
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Install react-router-dom and declare CachedCard type</name>
  <files>package.json, package-lock.json, src/types/card.ts</files>
  <read_first>
    - package.json (confirm current deps; react-router-dom MUST NOT already be present)
    - src/types/deck.ts (existing type file style - no `any`, explicit interfaces)
  </read_first>
  <action>
    1. Run `npm install react-router-dom` (no version flag — resolves to the current 7.x). Verify with `npm ls react-router-dom` that the installed version starts with `7.`.
    2. Create file `src/types/card.ts` with the exact contents below (no extra exports, no `any` casts):

    ```typescript
    import type { ScryfallCard } from '@scryfall/api-types';

    export interface CachedCard {
      /** Scryfall oracle_id — primary key in Dexie cards store. */
      oracle_id: string;
      /** Full Scryfall card JSON as returned by /cards endpoints. */
      cardJson: ScryfallCard.Any;
      /** Date.now() timestamp when this record was written to the cache. */
      cachedAt: number;
    }
    ```

    3. Do NOT update `src/lib/db.ts` in this task — Task 2 handles the Dexie migration.
  </action>
  <verify>
    <automated>npm ls react-router-dom --depth=0 &amp;&amp; npx tsc -p tsconfig.app.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "\"react-router-dom\": \"\\^?7\\." package.json` returns a match
    - `test -f src/types/card.ts` exits 0
    - `grep "export interface CachedCard" src/types/card.ts` returns a match
    - `grep "oracle_id: string" src/types/card.ts` returns a match
    - `grep "cardJson: ScryfallCard.Any" src/types/card.ts` returns a match
    - `grep "cachedAt: number" src/types/card.ts` returns a match
    - `npx tsc -p tsconfig.app.json --noEmit` exits 0
    - `node -e "const p=require('./package.json'); process.exit(p.dependencies['react-router-dom']?0:1)"` exits 0
  </acceptance_criteria>
  <done>
    react-router-dom ^7.x is in dependencies, `src/types/card.ts` exports `CachedCard` with the three required fields, TypeScript compiles.
  </done>
</task>

<task type="auto">
  <name>Task 2: Bump Dexie to v2 with cards store and add migration tests</name>
  <files>src/lib/db.ts, src/lib/db.test.ts</files>
  <read_first>
    - src/lib/db.ts (current v1 schema — Task 1's work did not change this file)
    - src/lib/db.test.ts (existing 6 tests — must continue to pass)
    - src/types/card.ts (CachedCard type introduced in Task 1)
    - src/test/setup.ts (confirms fake-indexeddb/auto is imported)
  </read_first>
  <action>
    1. Edit `src/lib/db.ts`:
       - Add `import type { CachedCard } from '../types/card';` at the top (after existing imports).
       - Add a new property to the class: `cards!: Table<CachedCard, string>;` (string key type because oracle_id is a string primary key).
       - Inside the constructor, AFTER the existing `this.version(1).stores({...})` block (never modify v1), add:

         ```typescript
         this.version(2).stores({
           decks: '++id, updatedAt',
           deckCards: '++id, deckId, scryfallId',
           deckChanges: '++id, deckId, timestamp',
           cards: 'oracle_id, cachedAt',
         });
         ```

       Note: v2 must redeclare every v1 store verbatim. The new `cards` store schema is `'oracle_id, cachedAt'` — `oracle_id` is the primary key (no `++` prefix because it is a string outbound key), `cachedAt` is a secondary index for TTL queries.

    2. Edit `src/lib/db.test.ts` — keep every existing test; APPEND a new `describe('Dexie v2 migration', ...)` block containing:

       - Test A: `it('preserves v1 decks after v2 schema applied', async () =>  ...)` — creates a deck via `db.decks.add(...)` with fields matching `Deck` type, reads it back, asserts `name` matches.
       - Test B: `it('cards store accepts put and get by oracle_id', async () => ...)` — writes `{ oracle_id: 'test-oracle-1', cardJson: {} as ScryfallCard.Any, cachedAt: Date.now() }` via `db.cards.put(...)`, reads it back with `db.cards.get('test-oracle-1')`, asserts `cachedAt` is present.
       - Test C: `it('cards store supports where(cachedAt) range queries for TTL eviction', async () => ...)` — writes two cards with `cachedAt: 1000` and `cachedAt: 2000`, calls `db.cards.where('cachedAt').below(1500).toArray()`, asserts length is 1.

       Each test must `beforeEach(async () => { await db.delete(); await db.open(); })` — follow the existing pattern in the file. Use `import type { ScryfallCard } from '@scryfall/api-types'` if needed for the test card JSON.

    3. Do NOT delete or alter any existing test in `db.test.ts`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/db.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "this.version(1).stores" src/lib/db.ts` returns exactly one match
    - `grep -n "this.version(2).stores" src/lib/db.ts` returns exactly one match
    - `grep "cards: 'oracle_id, cachedAt'" src/lib/db.ts` returns a match
    - `grep "cards!: Table<CachedCard, string>" src/lib/db.ts` returns a match
    - `grep "import type { CachedCard }" src/lib/db.ts` returns a match
    - `grep "Dexie v2 migration" src/lib/db.test.ts` returns a match
    - `grep "preserves v1 decks" src/lib/db.test.ts` returns a match
    - `grep "cards store accepts put and get" src/lib/db.test.ts` returns a match
    - `grep "where('cachedAt').below" src/lib/db.test.ts` returns a match
    - `npx vitest run src/lib/db.test.ts` reports all tests passing (including the 6 pre-existing tests and 3 new migration tests, totaling 9)
  </acceptance_criteria>
  <done>
    Dexie v2 schema declared additively, CachedCard-compatible `cards` Table exposed on the db instance, 3 new migration tests plus all 6 existing tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create Wave 0 test scaffolds for Wave 1 and Wave 2 modules</name>
  <files>src/lib/partner-detection.test.ts, src/lib/scryfall-client.test.ts, src/lib/card-cache.test.ts, src/store/commander-store.test.ts, src/store/card-search-store.test.ts, src/components/DeckWorkspace.test.tsx</files>
  <read_first>
    - .planning/phases/02-commander-selection-card-search/02-RESEARCH.md (test matrices and mock patterns — the "Phase Requirements → Test Map" and "What to Mock" sections)
    - .planning/phases/02-commander-selection-card-search/02-VALIDATION.md (Wave 0 Requirements list)
    - src/store/deck-store.test.ts (existing store test style — patterns to match)
    - src/components/DeckList.test.tsx (existing RTL test style)
    - src/test/setup.ts (confirms jest-dom + fake-indexeddb autoload)
  </read_first>
  <behavior>
    Each test file must compile, must import from the future module path (imports will RESOLVE once Wave 1/2 creates them), and each `describe` block must contain at least one `it.todo(...)` or `it(...)` with `expect(true).toBe(false)` marking the test as intentionally pending RED. Pending tests exist so Wave 1/2 tasks have a real `<automated>` target; Wave 1/2 replaces `it.todo` with real assertions.

    Test file contracts (exhaustive list of `it.todo` names required):

    **src/lib/partner-detection.test.ts** — covers CMDR-04, CMDR-05
    - detects generic Partner from keywords[]
    - detects Partner with <Name> from oracle_text with correct name capture
    - detects Friends Forever from keywords[]
    - detects Choose a Background from keywords[]
    - returns kind: none for non-partner commanders like Atraxa
    - areCompatiblePartners: generic + generic = true
    - areCompatiblePartners: generic + friendsForever = false
    - areCompatiblePartners: named + matching secondary name = true
    - areCompatiblePartners: named + non-matching secondary name = false
    - areCompatiblePartners: chooseBackground + Background-type secondary = true
    - areCompatiblePartners: chooseBackground + non-Background secondary = false

    **src/lib/scryfall-client.test.ts** — covers CMDR-01, SRCH-01..04, SRCH-06
    - searchCards fires GET /cards/search with correct URLSearchParams (q, unique=cards, order=name, page)
    - searchCards includes Accept: application/json header
    - searchCards returns parsed ScryfallList on 200
    - searchCards returns empty list shape (has_more=false, data=[]) on 404
    - searchCards throws on other 4xx/5xx
    - buildSearchQuery composes n:, t:, o:, id<=, f:commander correctly
    - buildSearchQuery uses "c" for empty colorIdentity (colorless commander)
    - getImageUri returns image_uris.normal for single-faced cards
    - getImageUri falls back to card_faces[0].image_uris.normal for DFC
    - getImageUri returns placeholder for cards with neither
    - rate limiter enforces 100ms gap between requests (use vi.useFakeTimers)
    - rate limiter aborts cleanly when AbortSignal fires mid-wait
    - __resetRateLimit() resets lastRequestTime to 0

    **src/lib/card-cache.test.ts** — covers cache read-through, TTL, bulkPut
    - getCard returns cached JSON when within TTL (7 days)
    - getCard returns null when cache entry older than 7 days
    - getCard returns null when oracle_id not in cache
    - cacheCard writes a single card with current cachedAt
    - cacheCard skips cards with no oracle_id (returns without throwing)
    - cacheCards bulkPuts every card with an oracle_id
    - cacheCards filters out cards missing oracle_id

    **src/store/commander-store.test.ts** — covers CMDR-02, CMDR-03
    - setCommander writes commanderId, commanderName, colorIdentity to db.decks row
    - setCommander updates primaryCommander in store state
    - setCommander clears partnerCommander when new primary is not partner-eligible
    - clearCommander resets both primary and partner and nulls deck row fields
    - setPartner writes nothing to db.decks (partner is UI-only in Phase 2) but updates store state
    - clearPartner resets partnerCommander to null

    **src/store/card-search-store.test.ts** — covers SRCH-05, SRCH-07
    - search() calls scryfall-client searchCards with a query string
    - search() populates results on success and sets status to 'success'
    - search() aborts the in-flight request when called again before first resolves
    - search() ignores AbortError and does not set error status
    - loadMore() increments currentPage and appends to results (not replaces)
    - loadMore() sets hasMore=false when response has_more is false
    - reset() clears query, results, currentPage, and sets status to 'idle'

    **src/components/DeckWorkspace.test.tsx** — covers routing + not-found
    - renders "Deck not found" when URL id has no matching deck (after loading=false)
    - shows loading state while useDeckStore loading=true
    - renders WorkspaceHeader, CommanderPanel, CardSearchSection stubs for a valid deck id
    - "Back to decks" link has href="/"
  </behavior>
  <action>
    For EACH file below, create it with this exact structure (replace MODULE_PATH and TEST_NAMES as indicated):

    ```typescript
    import { describe, it } from 'vitest';
    // NOTE: import path is forward-declared — Wave 1/2 implements these modules.
    // This file is intentionally RED. Wave 1/2 replaces it.todo with real assertions.

    describe('<module name>', () => {
      it.todo('<test name from behavior list above>');
      // ... one it.todo per bullet in the behavior section
    });
    ```

    For DeckWorkspace.test.tsx use `import { describe, it } from 'vitest';` — no JSX yet; each test is `it.todo(...)` with a string. The file must still have the `.tsx` extension per convention (later RTL rendering tests need JSX).

    DO NOT implement actual test assertions. DO NOT create Wave 1/2 source files. The goal is exhaustive `it.todo` coverage so downstream waves have an `<automated>` target and a clear checklist.

    File-by-file explicit action:

    1. Create `src/lib/partner-detection.test.ts` with `describe('partner-detection', ...)` containing `it.todo(...)` for each of the 11 bullets under partner-detection above.
    2. Create `src/lib/scryfall-client.test.ts` with `describe('scryfall-client', ...)` containing `it.todo(...)` for each of the 13 bullets under scryfall-client above.
    3. Create `src/lib/card-cache.test.ts` with `describe('card-cache', ...)` containing `it.todo(...)` for each of the 7 bullets under card-cache above.
    4. Create `src/store/commander-store.test.ts` with `describe('commander-store', ...)` containing `it.todo(...)` for each of the 6 bullets under commander-store above.
    5. Create `src/store/card-search-store.test.ts` with `describe('card-search-store', ...)` containing `it.todo(...)` for each of the 7 bullets under card-search-store above.
    6. Create `src/components/DeckWorkspace.test.tsx` with `describe('DeckWorkspace', ...)` containing `it.todo(...)` for each of the 4 bullets under DeckWorkspace above.
  </action>
  <verify>
    <automated>npx vitest run src/lib/partner-detection.test.ts src/lib/scryfall-client.test.ts src/lib/card-cache.test.ts src/store/commander-store.test.ts src/store/card-search-store.test.ts src/components/DeckWorkspace.test.tsx --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/lib/partner-detection.test.ts` exits 0
    - `test -f src/lib/scryfall-client.test.ts` exits 0
    - `test -f src/lib/card-cache.test.ts` exits 0
    - `test -f src/store/commander-store.test.ts` exits 0
    - `test -f src/store/card-search-store.test.ts` exits 0
    - `test -f src/components/DeckWorkspace.test.tsx` exits 0
    - `grep -c "it.todo" src/lib/partner-detection.test.ts` returns >= 11
    - `grep -c "it.todo" src/lib/scryfall-client.test.ts` returns >= 13
    - `grep -c "it.todo" src/lib/card-cache.test.ts` returns >= 7
    - `grep -c "it.todo" src/store/commander-store.test.ts` returns >= 6
    - `grep -c "it.todo" src/store/card-search-store.test.ts` returns >= 7
    - `grep -c "it.todo" src/components/DeckWorkspace.test.tsx` returns >= 4
    - `npx vitest run <the six files>` exits 0 (it.todo does not fail; it reports as skipped/pending)
    - `npm test` exits 0 overall (entire suite green including Wave 0 todos as pending + all Phase 1 tests + new db migration tests)
  </acceptance_criteria>
  <done>
    Six RED/pending test files exist with exhaustive `it.todo` coverage matching the behavior list. Vitest reports every todo as pending (not failing). Downstream waves have concrete verification targets.
  </done>
</task>

</tasks>

<verification>
- `npm test` — entire suite green; no regression in Phase 1 tests; new db migration tests green; Wave 0 test files report pending todos (not failures)
- `npx tsc -p tsconfig.app.json --noEmit` — no TypeScript errors
- `npm run build` — build exits 0 (Dexie v2 change compiles)
- `node -e "const p=require('./package.json'); console.log(p.dependencies['react-router-dom'])"` — prints a ^7.x version string
- `grep "this.version(2).stores" src/lib/db.ts` — returns a match
</verification>

<success_criteria>
- react-router-dom ^7.x in package.json dependencies (T-02-01-A mitigated via canonical npm name + version verification)
- Dexie class exposes `cards!: Table<CachedCard, string>` alongside the existing three tables; v2 schema additive-only (T-02-01-B mitigated)
- All 9 db tests pass (6 existing + 3 new migration)
- Six Wave 0 test files exist with exhaustive `it.todo` coverage matching 02-VALIDATION.md
- Entire vitest suite exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/02-commander-selection-card-search/02-01-SUMMARY.md`
</output>
