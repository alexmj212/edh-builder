---
phase: 02.2-playwright-e2e-harness
plan: 1
subsystem: testing
tags: [playwright, e2e, typescript, vite, chromium]

# Dependency graph
requires:
  - phase: 02.1-partner-persistence
    provides: Commander partner UI flows that the E2E harness will cover in Plans 02-04

provides:
  - "@playwright/test 1.59.1 installed as devDependency"
  - "playwright.config.ts with webServer, retry/trace policy, chromium project"
  - "tsconfig.e2e.json scoped to e2e/ with strict types + @playwright/test globals"
  - "Vite dev-server deterministically pinned to port 5173 (strictPort)"
  - "npm scripts: e2e, e2e:ui, e2e:install, typecheck:e2e"
  - ".gitignore entries blocking playwright-report/, test-results/, /blob-report/, /e2e/.auth/"
  - "e2e/README.md skeleton with folder layout, conventions, console policy, and Plan 05 PLACEHOLDER"

affects:
  - "02.2-02 (smoke spec)"
  - "02.2-03 (partner backfill specs)"
  - "02.2-04 (stubScryfall fixture + helpers)"
  - "all subsequent phases: every phase must ship E2E coverage per standing rule"

# Tech tracking
tech-stack:
  added:
    - "@playwright/test@^1.59.1 (chromium-only, headless)"
  patterns:
    - "Playwright webServer wires to npm run dev (not vite preview) — real dev server path"
    - "reuseExistingServer: !process.env.CI — local reuse, CI always fresh"
    - "tsconfig.e2e.json stands alone (not in tsconfig references) — tsc -b build path unchanged"
    - "strictPort: true on Vite keeps playwright baseURL deterministic"

key-files:
  created:
    - "playwright.config.ts — test runner config with webServer + chromium project"
    - "tsconfig.e2e.json — strict typecheck for e2e/ extending tsconfig.app.json"
    - "e2e/README.md — folder conventions, running instructions, standing-rule placeholder"
    - "e2e/specs/ — empty directory; testDir target for Playwright"
  modified:
    - "package.json — added e2e, e2e:ui, e2e:install, typecheck:e2e scripts + @playwright/test devDep"
    - "vite.config.ts — added server.port=5173, server.strictPort=true"
    - ".gitignore — added Playwright artifact entries"
    - "package-lock.json — updated for @playwright/test"

key-decisions:
  - "chromium-only in v1 for speed; multi-browser deferred (per CONTEXT D-04)"
  - "tsconfig.e2e.json stands alone, NOT added to tsconfig.json references — tsc -b build path untouched"
  - "allowImportingTsExtensions overridden to false in tsconfig.e2e.json — Playwright runner not Vite"
  - "Browser install via npm run e2e:install one-liner, no postinstall hook (RESEARCH recommendation)"
  - "Playwright --list exits 1 with 'No tests found' in v1.59.x for empty suites — config parses correctly"

patterns-established:
  - "All api.scryfall.com/* traffic will be intercepted via page.route() (D-05) — set up in Plan 02"
  - "e2e/specs/, e2e/fixtures/, e2e/helpers/ folder layout (D-02)"
  - "Specs reach starting state via UI driving, not direct Dexie injection (D-10)"

requirements-completed: [TEST-01]

# Metrics
duration: 3min
completed: 2026-04-12
---

# Phase 02.2 Plan 01: Playwright E2E Harness Skeleton Summary

**@playwright/test 1.59.1 installed with chromium project, webServer wired to npm run dev at port 5173 (strictPort), dedicated tsconfig.e2e.json, and e2e/README.md skeleton with Plan 05 standing-rule placeholder**

## Performance

- **Duration:** 3 min
- **Started:** 2026-04-12T15:11:22Z
- **Completed:** 2026-04-12T15:14:xx Z
- **Tasks:** 4/4
- **Files modified:** 7

## Accomplishments

- Playwright 1.59.1 installed as devDependency; chromium binary downloaded to ~/.cache/ms-playwright
- playwright.config.ts with canonical webServer block (npm run dev, localhost:5173, reuseExistingServer: !CI, retries: 2 in CI, trace/video retain-on-failure)
- tsconfig.e2e.json extends tsconfig.app.json with @playwright/test types, scoped to e2e/**/* — stands alone outside tsconfig references so tsc -b is unaffected
- Vite server.port=5173 + strictPort=true eliminates port-binding ambiguity between Playwright baseURL and dev server
- .gitignore blocks playwright-report/, test-results/, /blob-report/, /e2e/.auth/ to prevent artifact leakage
- e2e/README.md skeleton (72 lines) documents folder layout, running instructions, conventions, console policy, and PLACEHOLDER for Plan 05 standing-rule text

## Task Commits

Each task was committed atomically:

1. **Task 1: Install @playwright/test and add package scripts** — `c55e7c0` (chore)
2. **Task 2: Write playwright.config.ts skeleton** — `daf8e4d` (chore)
3. **Task 3: Create tsconfig.e2e.json and pin Vite port** — `dd0d2b4` (chore)
4. **Task 4: Create .gitignore entries and e2e/README.md skeleton** — `067d414` (chore)

## Files Created/Modified

- `playwright.config.ts` — Playwright test runner config: webServer (npm run dev, port 5173), chromium project, retry/trace policy
- `tsconfig.e2e.json` — Strict TypeScript config for e2e/ extending tsconfig.app.json; adds node + @playwright/test types
- `e2e/README.md` — Folder layout, how to run, conventions (no real network, UI-driven setup, role/name locators), console policy, Plan 05 PLACEHOLDER
- `e2e/specs/` — Empty directory created as testDir target
- `package.json` — Added scripts (e2e, e2e:ui, e2e:install, typecheck:e2e) + @playwright/test devDep
- `vite.config.ts` — Added server.port=5173 + server.strictPort=true
- `.gitignore` — Added Playwright artifact entries

## Decisions Made

- chromium-only in v1 for speed; Firefox/WebKit deferred per CONTEXT D-04
- tsconfig.e2e.json intentionally NOT added to tsconfig.json references array — tsc -b build semantics unchanged
- `allowImportingTsExtensions: false` overrides inherited `true` from tsconfig.app.json because Playwright runner (not Vite bundler) processes e2e/ files
- No postinstall hook for browser download — documented as `npm run e2e:install` one-liner per RESEARCH.md recommendation
- Verified @playwright/test originates from microsoft/playwright org (T-01 mitigation)

## Deviations from Plan

### Noted Behavior Difference

**Playwright 1.59.x `--list` returns exit code 1 for empty suites**

- **Found during:** Plan-level verification
- **Issue:** `npx playwright test --list` exits 1 with "No tests found" (0 files) rather than exit 0 as the plan acceptance criteria states
- **Assessment:** This is correct Playwright 1.59.x behavior — the config parses without errors (no "Error loading config" or similar), and "No tests found" is the expected empty-suite message. Once Plan 02 adds specs, `--list` will exit 0.
- **Action:** No fix needed — the config is correct. The plan criterion "exits 0 (empty suite OK; config must parse)" is satisfied in substance: config parses, no errors, just zero test files.

---

**Total deviations:** 0 auto-fixes required. One noted behavior difference in verification (Playwright exit code for empty suite — cosmetic, not a defect).

## Issues Encountered

- `npx playwright install --with-deps chromium` requires sudo for OS-level deps (WSL2 environment). Used `npx playwright install chromium` instead to download the browser binary only. The `npm run e2e:install` script retains `--with-deps` for environments where sudo is available (CI, fresh machines).

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- Harness is fully functional; `npm run e2e` runs without config errors, `npm run typecheck:e2e` passes
- Plan 02 can immediately add `e2e/specs/00-smoke.spec.ts` and `e2e/helpers/stubScryfall.ts`
- The `e2e/specs/` directory exists as the `testDir` target
- No blockers for Plans 02-05

---
*Phase: 02.2-playwright-e2e-harness*
*Completed: 2026-04-12*
