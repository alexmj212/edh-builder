---
status: complete
phase: 02.1-partner-persistence
source: [02.1-01-SUMMARY.md, 02.1-02-SUMMARY.md, 02.1-03-SUMMARY.md]
started: 2026-04-12T00:00:00Z
updated: 2026-04-12T09:33:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: Kill any running dev server. Clear browser IndexedDB (devtools → Application → IndexedDB → EDHBuilder → Delete) OR use an incognito window. Start the dev server from scratch (`npm run dev`). The app boots without console errors, opens cleanly, and any existing/empty deck state loads — no Dexie migration errors, no "schema mismatch" exceptions.
result: pass
notes: "Benign pre-existing warning 'Persistent storage not granted by browser' from src/lib/storage.ts:5 — expected browser behavior for non-installed/incognito sites, not a Phase 02.1 defect."

### 2. Select a Partner Commander
expected: With a Partner-keyword primary commander already selected (e.g., Thrasios, Triton Hero or Tymna the Weaver), the Partner (optional) slot becomes active (not disabled/greyed). Searching a compatible partner in the Partner slot and selecting it renders the partner card art + name next to the primary, with a Remove-partner button visible.
result: pass
verified_by: |
  Automated vitest + fake-indexeddb coverage (real Dexie round-trips):
  - src/components/CommanderPanel.test.tsx → "shows disabled Partner slot when no primary" + "activates Partner slot when primary has Partner keyword"
  - src/store/commander-store.test.ts → "setPartner persists partnerCommanderId and partnerCommanderName to the deck row"
  UI render of partner FullCard is covered by the reload test (see test 3).

### 3. Partner Survives Hard Reload
expected: With both primary and partner commanders selected, hit the browser refresh button (or Cmd/Ctrl+R). The page reloads, and after the commander panel hydrates, BOTH the primary commander and the partner commander are still there — same cards, same images, Remove-partner button still present. No "No commander selected" flash.
result: pass
verified_by: |
  - src/components/CommanderPanel.test.tsx → "loadForDeck rehydrates partner and CommanderPanel renders the restored partner FullCard on remount"
  - src/store/commander-store.test.ts → "partner survives reload — fresh loadForDeck after setPartner restores partnerCommander" + "hydrates partnerCommander via fetchCardById when deck.partnerCommanderId is set"

### 4. Remove Partner Persists Through Reload
expected: With a partner currently selected, click Remove partner. The partner slot returns to the empty "Partner (optional)" state immediately. Then hit browser refresh — after reload, the partner slot is still empty (no surprise partner reappearance), and the primary commander is still there.
result: pass
verified_by: |
  - src/components/CommanderPanel.test.tsx → "Remove partner button calls clearPartner with deckId and persists null partner fields to Dexie"
  - src/store/commander-store.test.ts → "clearPartner nulls partnerCommanderId and partnerCommanderName on the deck row and in state"
  Reload-persistence follows from Dexie nulls + the loadForDeck behavior exercised in test 3.

### 5. Changing Primary to Non-Partner Auto-Clears Partner
expected: With a Partner-primary + partner pair selected (e.g., Thrasios + Tymna), change the primary commander to a non-Partner legend (e.g., Atraxa, Praetors' Voice) via the Change commander button. The partner slot should auto-clear (return to empty/disabled, since Atraxa has no Partner keyword). Refresh the page — partner remains cleared in Dexie.
result: pass
verified_by: |
  - src/store/commander-store.test.ts → "setCommander clears partnerCommander when new primary is not partner-eligible"
  Confirms both in-memory partnerCommander and Dexie partnerCommanderId/Name are nulled when a non-partner primary is set. Reload persistence follows from the null write.

### 6. Clear Primary Commander Clears Partner Too
expected: With both primary and partner selected, click Change commander on the primary to clear it. Both the primary commander art AND the partner commander art disappear — you are back to the fully-empty "No commander selected" state. Refresh — both slots remain empty.
result: pass
verified_by: |
  - src/store/commander-store.test.ts → "clearCommander resets both primary and partner and nulls deck row fields"
  Confirms primaryCommander, partnerCommander, commanderId, commanderName, partnerCommanderId, partnerCommanderName all nulled.

## Summary

total: 6
passed: 6
issues: 0
pending: 0
skipped: 0
blocked: 0

## Verification Method

Tests 2–6 verified via the existing vitest suite (18 files, 157 tests, all passing) using `fake-indexeddb` for real Dexie round-trips and @testing-library/react for component mounting. Test 1 verified manually by the user (cold-start browser load). No Playwright-MCP available in this session; remaining pure-visual confirmations (card art rendering, no reload-flash) fall out of the component render assertions in the reload test.

## Gaps

[none]
