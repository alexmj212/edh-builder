---
phase: 02.3-scryfall-api-migration
verified: 2026-04-12T20:59:00Z
status: passed
score: 11/11 must-haves verified
re_verification:
  previous_status: none
  note: "Initial verification — no prior VERIFICATION.md existed."
---

# Phase 02.3: scryfall-api Migration — Verification Report

**Phase Goal:** Replace the hand-rolled `src/lib/scryfall-client.ts` and the stale
`@scryfall/api-types@1.0.0-alpha.4` types with the actively-maintained
`scryfall-api` (MarioMH8) package. Restore `tsc -b` in the build pipeline, drop
the `tsconfig.e2e.json` strict-flag overrides, and eliminate the
`as unknown as ScryfallCard.Any` casts. Closes REVIEW.md WR-01 from Phase 02.2.

**Verified:** 2026-04-12
**Status:** passed
**Re-verification:** No — initial verification.

---

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                                                      | Status     | Evidence                                                                                                                                                |
| --- | -------------------------------------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `scryfall-api@^4.0.5` is installed and declared as a dependency                                                            | VERIFIED   | `package.json` contains `"scryfall-api": "^4.0.5"` under dependencies.                                                                                  |
| 2   | `@scryfall/api-types` is fully removed from `package.json` and `package-lock.json`                                         | VERIFIED   | `grep -F "@scryfall/api-types" package.json package-lock.json` returns zero matches.                                                                    |
| 3   | `src/lib/scryfall-client.ts` and `src/lib/scryfall-client.test.ts` are deleted                                             | VERIFIED   | `ls` for both paths returns "No such file or directory". `grep -rn "scryfall-client"` across repo (excluding `.planning/`, `node_modules/`) returns 0. |
| 4   | `npm run build` runs `tsc -b && vite build` cleanly                                                                        | VERIFIED   | `package.json` `build` script = `"tsc -b && vite build"`. Running `npm run build` exits 0; `vite build` reports `✓ built in 357ms`.                      |
| 5   | No `@scryfall/api-types` or `ScryfallCard` references remain in `src/` or `e2e/`                                           | VERIFIED   | `rg "@scryfall/api-types\|ScryfallCard" src/` → no matches. `rg "@scryfall/api-types\|ScryfallCard" e2e/` → no matches.                                 |
| 6   | No `as unknown as ScryfallCard.Any` casts remain in production code                                                         | VERIFIED   | `rg "as unknown as ScryfallCard" src/ e2e/` → no matches. Remaining `as unknown as` occurrences are in `*.test.ts` fixtures or the single `asCard` helper. |
| 7   | `tsconfig.e2e.json` strict-flag overrides are removed                                                                      | VERIFIED   | File contains no `verbatimModuleSyntax` or `erasableSyntaxOnly` keys. Extends `./tsconfig.app.json` directly.                                          |
| 8   | `.github/workflows/e2e.yml` has a `Typecheck` step that runs before Playwright                                             | VERIFIED   | Lines 38-39: `- name: Typecheck (src + e2e)` / `run: npm run typecheck && npm run typecheck:e2e`, positioned between `npm ci` and `playwright install`. |
| 9   | All existing unit tests pass against the new wrapper                                                                       | VERIFIED   | `npm test -- --run` → 19 test files, 170 tests passed, exit 0. Duration 2.59s.                                                                          |
| 10  | `e2e/helpers/stubScryfall.ts` uses a `Pick<Card, ...>` FixtureCard + single `asCard` helper (three call sites route through it) | VERIFIED   | Line 18 defines `Pick<Card, ...>`; line 42 defines `asCard`; three call sites at lines 45-47 (`asCard(thrasios)`, `asCard(tymna)`, `asCard(atraxa)`).   |
| 11  | WR-01 from Phase 02.2 REVIEW.md is fully closed (root cause + CI gate)                                                     | VERIFIED   | Root cause: `tsc -b` restored in build script (truth 4). CI gate: Typecheck step added (truth 8). Both halves present.                                   |

**Score:** 11/11 truths verified.

---

### Required Artifacts

| Artifact                                              | Expected                                                      | Status   | Details                                                                                                                                           |
| ----------------------------------------------------- | ------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/lib/scryfall.ts`                                 | Thin wrapper; sole importer of `scryfall-api`; Zod boundary   | VERIFIED | Exists; imports from `'scryfall-api'`; exports `abortable`, `searchCards`, `fetchNextPage`, `fetchCardById`, `searchCommanders`, `searchPartnersFor`, `getImageUri`, `SearchResult`, `Card`. |
| `src/lib/scryfall-queries.ts`                         | Pure query builders; zero `scryfall-api` imports              | VERIFIED | Exists; exports `encodeFragment`, `buildSearchQuery`, `buildCommanderSearchQuery`, `buildPartnerQuery`. `detectPartnerType(primary)` called without cast. |
| `src/lib/scryfall.test.ts`                            | Unit tests for wrapper                                        | VERIFIED | Exists; tests run under Vitest and pass (part of the 170-test suite).                                                                             |
| `src/lib/scryfall-queries.test.ts`                    | Unit tests for query builders                                 | VERIFIED | Exists; tests pass.                                                                                                                               |
| `scripts/scryfall-probe.mjs`                          | Live Scryfall probe, UA identified                            | VERIFIED | Exists; `grep -c "edh-builder/02.3-migration-probe"` = 2.                                                                                          |
| `tsconfig.e2e.json`                                   | Clean, no alpha-package overrides                             | VERIFIED | No `verbatimModuleSyntax` / `erasableSyntaxOnly`. Inherits from `tsconfig.app.json`.                                                              |
| `tsconfig.app.json` (implicit — no temporary exclude) | No residual `exclude` for `scryfall-client.{ts,test.ts}`      | VERIFIED | Phase 02.3-05 removed the temporary exclude after deleting the files (per REVIEW §Strengths).                                                    |
| `.github/workflows/e2e.yml`                           | `Typecheck` step before Playwright                            | VERIFIED | Present at lines 38-39; positioned AFTER `npm ci` and BEFORE `playwright install`.                                                                |
| `e2e/helpers/stubScryfall.ts`                         | `Pick<Card>` + single `asCard`; 3 call sites through it       | VERIFIED | `Pick<Card, …>` at line 18; single `asCard = (f: object): Card => f as unknown as Card` at line 42; three `asCard(…)` call sites.                |
| `e2e/specs/07-card-search.spec.ts`                    | Playwright regression spec (SRCH-01..07, Load-more)           | VERIFIED | Exists; imports page1/page2 fixtures; stateful rotation for Load-more click.                                                                     |
| `e2e/fixtures/searches/card-search-sample.json`       | Page 1 stub (has_more: true, 2 cards)                         | VERIFIED | Exists.                                                                                                                                           |
| `e2e/fixtures/searches/card-search-sample-page2.json` | Page 2 stub (has_more: false, 2 distinct cards)               | VERIFIED | Exists.                                                                                                                                           |
| `package.json`                                        | `build: "tsc -b && vite build"`; `scryfall-api ^4.0.5`; no `@scryfall/api-types` | VERIFIED | All three conditions hold.                                                                                                                        |

---

### Key Link Verification

| From                                       | To                                              | Via                                       | Status | Details                                                                                                 |
| ------------------------------------------ | ----------------------------------------------- | ----------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------- |
| `src/lib/scryfall.ts`                      | `scryfall-api` package                          | `import { Cards, MagicPageResult, … }`    | WIRED  | Single module importing from `'scryfall-api'` in `src/`.                                                |
| `src/store/card-search-store.ts`           | `src/lib/scryfall.ts`                           | `import { searchCards, fetchNextPage … }` | WIRED  | No `scryfall-client` residue; tests pass.                                                               |
| `src/store/commander-store.ts`             | `src/lib/scryfall.ts`                           | `import { fetchCardById, type Card }`     | WIRED  | Tests pass.                                                                                             |
| `src/lib/card-cache.ts`                    | `src/lib/scryfall.ts`                           | `import type { Card }`                    | WIRED  | Tests pass.                                                                                             |
| `src/lib/partner-detection.ts`             | `src/lib/scryfall.ts`                           | `import type { Card }`                    | WIRED  | `detectPartnerType(primary)` called without `as never` cast — confirms re-typed surface.                |
| `e2e/helpers/stubScryfall.ts`              | `src/lib/scryfall.ts` Card type                 | `import type { Card } from '../../src/lib/scryfall'` | WIRED  | Present at line 4.                                                                                      |
| `.github/workflows/e2e.yml` Typecheck step | `npm run typecheck` + `npm run typecheck:e2e`   | GHA `run:` line                           | WIRED  | Line 39.                                                                                                |
| `package.json build`                       | `tsc -b` typecheck gate                         | `tsc -b && vite build`                    | WIRED  | Build script restored; `npm run build` exits 0.                                                         |
| `e2e/specs/07-card-search.spec.ts`         | `card-search-sample-page2.json`                 | Stateful route rotation                   | WIRED  | Spec imports both page1/page2 fixtures and rotates on 2nd hit for Load-more click assertion.            |

All key links verified.

---

### Requirements Coverage

REQUIREMENTS.md does not directly map requirement IDs to Phase 02.3 (no `Phase 02.3` table row found). Requirements are declared in each plan's frontmatter and cross-checked against code artifacts below.

| Requirement | Source Plan(s)     | Description                                                | Status     | Evidence                                                                                      |
| ----------- | ------------------ | ---------------------------------------------------------- | ---------- | --------------------------------------------------------------------------------------------- |
| SRCH-01..07 | 02, 03, 05         | Card search (name, type, oracle, color, pagination, abort) | SATISFIED  | `scryfall.test.ts` + `scryfall-queries.test.ts` cover builders & wrapper; Playwright `07-card-search.spec.ts` covers SRCH-01..07 E2E. All tests green. |
| CMDR-01..05 | 01, 02, 03, 05     | Commander search + partner detection                       | SATISFIED  | `commander-store.ts` + `partner-detection.ts` migrated to `Card`; existing commander tests green. |
| TEST-01     | 05                 | E2E coverage for phase                                     | SATISFIED  | `e2e/specs/07-card-search.spec.ts` added per Phase 02.2-05 standing rule.                     |
| WR-01       | 04, 05             | Phase 02.2 WR-01 (silent `tsc` drop)                       | SATISFIED  | Build script restored to `tsc -b && vite build`; CI `Typecheck` step added.                   |
| D-01..D-10  | 01, 02             | Design decisions (throttle, abort, pagination, Zod, …)     | SATISFIED  | Ratified in `02.3-01-SUMMARY.md` spike PASS; wrapper implements promise-race abort + Zod boundary; library provides module-global 100ms throttle. |
| D-06        | 01, 04             | Vite/ESM compat                                            | SATISFIED  | `npm run build` green; ESM import resolves.                                                   |
| D-07        | 01, 03             | Card field parity                                          | SATISFIED  | Live probe `missingFields: []`; type sweep compiles cleanly (`tsc -b` green).                 |

No orphaned requirements — every declared requirement has corresponding artifacts and passing tests.

---

### Anti-Patterns Found

Full repo scan (`src/`, `e2e/`) for TODO/FIXME/placeholder/empty-implementation patterns:

| File                                  | Pattern                          | Severity | Impact                                                                                                                                                                                                       |
| ------------------------------------- | -------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `src/lib/scryfall.ts` (L46-49)        | `validatedData` discards Zod output, casts raw | Info (REVIEW WR-1) | Latent risk if schema tightens (`.transform()` / `.strip()`); not a current bug with `.passthrough()`. Non-blocking.                                                                                          |
| `src/lib/scryfall.ts` (L107-113)      | `Cards.search('')` for 'none' partner | Info (REVIEW WR-2) | Would HTTP-fail if `fetchNextPage` were ever called on the handle; contained by `hasMore: false` store guard. Non-blocking.                                                                                   |
| `src/lib/scryfall.test.ts`            | Missing negative test for Zod boundary | Info (REVIEW WR-3) | D-05 Zod validation has no test for ZodError propagation; happy path only. Non-blocking, recommended follow-up.                                                                                               |
| `src/lib/scryfall-queries.ts` (L7-11) | Dead `"` in wrapping-decision regex  | Info (REVIEW WR-4) | `encodeFragment` strips all quotes before testing; the `"` in the regex cannot match. Readability defect only. Non-blocking.                                                                                 |

All four items are REVIEW-acknowledged warnings, explicitly non-blocking and non-critical. No blocker anti-patterns (no TODO/FIXME/placeholder markers in phase-modified production files).

---

### Automated Pipeline Evidence

- `npm run typecheck` → exit 0 (`tsc -b` clean across src/)
- `npm run typecheck:e2e` → exit 0 (`tsc -p tsconfig.e2e.json` clean)
- `npm run build` → exit 0 (`tsc -b && vite build`; `✓ built in 357ms`)
- `npm test -- --run` → exit 0 (19 files / 170 tests passed, 2.59s)

---

### Human Verification Required

None. All phase deliverables are verifiable programmatically (type sweep, file presence, script behavior). The live Scryfall probe and the Playwright E2E regression spec are CI-executable; the code review (`02.3-REVIEW.md`) already ratified functional behavior.

Optional follow-up (not blocking phase closure):

1. **REVIEW WR-3 gap-fill test:** Add one Vitest case feeding malformed data (card missing `id`) through `searchCards` and assert `ZodError` propagation. Closes the D-05 test gap.
2. **REVIEW WR-1 / WR-4 polish:** One-line Zod output-use fix and one-character regex cleanup. Trivial.
3. **REVIEW WR-2 hardening:** Replace `Cards.search('')` with an inert object cast in the 'none' partner branch.

These can be handled in a small follow-up plan or rolled into Phase 3 opening work.

---

### Gaps Summary

No gaps. All 11 observable truths verified, all 13 artifacts present and substantive, all 9 key links wired, full automated pipeline green (typecheck / typecheck:e2e / build / unit tests). The four REVIEW warnings are explicitly non-blocking latent defects (not incomplete deliverables) and do not affect the phase-goal verdict.

**Phase 02.3 goal achieved:**
- Hand-rolled `scryfall-client.ts` + test: deleted.
- Stale `@scryfall/api-types` dependency: fully removed from `package.json` + `package-lock.json`.
- `scryfall-api@^4.0.5`: installed, wrapped in `src/lib/scryfall.ts`, covered by unit + E2E tests.
- `tsc -b` restored in build pipeline.
- `tsconfig.e2e.json` strict-flag overrides: removed.
- `as unknown as ScryfallCard.Any` casts: eliminated in production code (test-only fixture casts remain, justified by Zod runtime boundary).
- WR-01 from Phase 02.2: fully closed (root cause + CI gate).

---

_Verified: 2026-04-12T20:59:00Z_
_Verifier: Claude (gsd-verifier)_
