---
phase: 02.3-scryfall-api-migration
plan: 05
type: execute
wave: 5
depends_on:
  - 02.3-04
files_modified:
  - src/lib/scryfall-client.ts
  - src/lib/scryfall-client.test.ts
  - package.json
  - package-lock.json
  - e2e/specs/07-card-search.spec.ts
  - e2e/fixtures/searches/card-search-sample.json
  - e2e/fixtures/searches/card-search-sample-page2.json
autonomous: true
requirements:
  - SRCH-01
  - SRCH-02
  - SRCH-03
  - SRCH-04
  - SRCH-05
  - SRCH-06
  - SRCH-07
  - CMDR-01
  - CMDR-02
  - CMDR-03
  - CMDR-04
  - CMDR-05
  - TEST-01
  - WR-01
gap_closure: false
must_haves:
  truths:
    - "A new Playwright regression spec exists at e2e/specs/07-card-search.spec.ts per the Phase 02.2-05 standing rule — covers card search result rendering AND pagination (clicks Load-more, asserts grid grows) against stubbed Scryfall"
    - "The Playwright spec was ADDED FIRST (Task 1) and confirmed green against the PRE-DELETION codebase (old scryfall-client.ts still present, new scryfall.ts wrapper also present) — this is the test-first gate per memory `feedback_test_first_workflow`"
    - "After the regression spec is green, src/lib/scryfall-client.ts is DELETED (Task 2)"
    - "After the regression spec is green, src/lib/scryfall-client.test.ts is DELETED (Task 2)"
    - "After the regression spec is green, @scryfall/api-types is REMOVED from package.json dependencies AND from package-lock.json (Task 2)"
    - "After the regression spec is green, `package.json` `build` script is restored to `tsc -b && vite build` (Task 2) — deferred here from plan 02.3-04 because src/lib/scryfall-client.ts deletion is a prerequisite for tsc -b passing against `tsconfig.app.json`'s `include: [\"src\"]` scope"
    - "Zero occurrences of `@scryfall/api-types`, `ScryfallCard`, or `scryfall-client` across the entire repo (excluding .planning/ documentation and node_modules/) after Task 2"
    - "The Playwright regression spec STILL passes post-deletion (Task 2 re-runs it — this is the real regression gate per memory `feedback_test_first_workflow`)"
    - "Full suite green: npm run typecheck, npm run typecheck:e2e, npm test, npm run build (tsc -b + vite build), npm run e2e all exit 0"
    - "Pagination (SRCH-07) is exercised E2E: spec clicks the Load-more button and asserts the results grid grows (either card count increases OR new card names become visible)"
    - "WR-01 is FULLY closed: build-script root cause (tsc -b restored) AND CI typecheck step (added in plan 02.3-04) both in place"
  artifacts:
    - path: "e2e/specs/07-card-search.spec.ts"
      provides: "Playwright regression — card search result grid renders, pagination loads more, grid grows after Load-more click"
      min_lines: 50
    - path: "e2e/fixtures/searches/card-search-sample.json"
      provides: "Stubbed Scryfall /cards/search PAGE 1 response (has_more: true, 2 cards)"
      min_lines: 10
    - path: "e2e/fixtures/searches/card-search-sample-page2.json"
      provides: "Stubbed Scryfall /cards/search PAGE 2 response (has_more: false, 2 additional distinct cards) — enables Load-more click assertion"
      min_lines: 10
    - path: "package.json"
      provides: "Restored build script (tsc -b && vite build) + @scryfall/api-types removed"
      contains: "tsc -b && vite build"
  key_links:
    - from: "e2e/specs/07-card-search.spec.ts"
      to: "e2e/helpers/stubScryfall.ts"
      via: "scryfallStub auto-fixture (test.extend)"
      pattern: "from '\\.\\./helpers/stubScryfall'"
    - from: "e2e/specs/07-card-search.spec.ts Load-more click"
      to: "card-search-sample-page2.json"
      via: "route.fulfill rotates page1 → page2 on follow-up request"
      pattern: "card-search-sample-page2"
    - from: "package.json build script"
      to: "src/ tsc -b typecheck gate"
      via: "restored after scryfall-client.ts deletion"
      pattern: "tsc -b &amp;&amp; vite build"
threat_model:
  - id: T-02.3-05-A
    category: regression
    severity: medium
    description: "Deleting scryfall-client without replacement coverage could leave a blind spot if new wrapper tests missed a case"
    mitigation: "Plan 02.3-02 ported query-builder tests to scryfall-queries.test.ts + created scryfall.test.ts with abortable/404/pagination coverage. Task 1 of THIS plan adds a Playwright card-search regression spec that must pass against the PRE-DELETION code; Task 2 re-runs after deletion — this is the real regression gate."
    block_on: high
  - id: T-02.3-05-B
    category: supply_chain
    severity: low
    description: "Removing @scryfall/api-types leaves only scryfall-api as the Scryfall type surface"
    mitigation: "scryfall-api is maintained (2025-12 release), MIT, zero runtime deps, pinned ^4.0.5. Zod validation at wrapper boundary is the runtime safety net."
    block_on: high
  - id: T-02.3-05-C
    category: build_gate
    severity: medium
    description: "Restoring `tsc -b && vite build` while src/lib/scryfall-client.ts still imports @scryfall/api-types would fail"
    mitigation: "Build-script restoration is in Task 2 AFTER scryfall-client.ts deletion, not before. Task order is deliberate."
    block_on: high
---

<execution_policy>
**Commit-as-you-go (per memory `feedback_commit_as_you_go`):** Executor commits after each task's `<verify>` passes. Commit messages follow `feat|fix|chore|refactor|test|ci|docs(02.3-05): <what>` convention. Working tree should be empty between tasks.

**Test-first ordering (per memory `feedback_test_first_workflow`):** Task order is deliberate — Task 1 adds the regression spec and confirms GREEN against the pre-deletion codebase. Task 2 then deletes the old client and re-runs the spec. The spec MUST still pass post-deletion; that is the real regression gate. Do NOT reorder these tasks.
</execution_policy>

<objective>
Phase 02.3 closeout. Execute in test-first order:

1. **Task 1 (TEST FIRST):** Add the Playwright card-search regression spec + page1/page2 fixtures. Confirm GREEN against the pre-deletion codebase (old `scryfall-client.ts` + `scryfall-client.test.ts` still present alongside the new `scryfall.ts` wrapper from plan 02). This establishes the regression baseline per memory `feedback_test_first_workflow`.

2. **Task 2 (DELETE + RESTORE BUILD SCRIPT):** Delete `src/lib/scryfall-client.ts` + `scryfall-client.test.ts`. Uninstall `@scryfall/api-types`. Restore `package.json` `build` script to `tsc -b && vite build` (deferred from plan 02.3-04 per checker W-2 Option A — this is the first moment `tsc -b` can succeed on `tsconfig.app.json`'s `include: ["src"]` scope). Re-run the full suite including the new Playwright spec. The spec MUST still pass — that's the real regression gate.

3. **Task 3 (CLOSEOUT SUMMARY):** Write `02.3-05-SUMMARY.md` documenting phase completion, WR-01 closure (both halves), and full-suite verification evidence.

Purpose: Phase closeout. After this plan, the codebase has ONE Scryfall type source (`scryfall-api`), the Phase 02.2 WR-01 warning is fully retired, and the Phase 02.2-05 standing rule (E2E coverage per phase) is satisfied.

Output: New Playwright spec + page2 fixture, deleted old files, restored build script, cleaned dependency tree, green full-suite evidence, closeout SUMMARY.
</objective>

<execution_context>
@/home/alex/.claude/get-shit-done/workflows/execute-plan.md
@/home/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02.3-scryfall-api-migration/02.3-CONTEXT.md
@.planning/phases/02.3-scryfall-api-migration/02.3-RESEARCH.md
@.planning/phases/02.3-scryfall-api-migration/02.3-01-SUMMARY.md
@.planning/phases/02.2-playwright-e2e-harness/02.2-05-SUMMARY.md
@e2e/specs/00-smoke.spec.ts
@e2e/specs/01-partner-activation.spec.ts
@e2e/helpers/stubScryfall.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Playwright card-search regression spec (TEST FIRST — confirm green against pre-deletion code)</name>
  <files>e2e/specs/07-card-search.spec.ts, e2e/fixtures/searches/card-search-sample.json, e2e/fixtures/searches/card-search-sample-page2.json</files>
  <read_first>
    - e2e/specs/00-smoke.spec.ts (skeleton + installConsoleGate pattern)
    - e2e/specs/01-partner-activation.spec.ts (commanderFlows usage)
    - e2e/helpers/stubScryfall.ts (current route handlers)
    - e2e/fixtures/searches/thrasios-search.json (shape reference)
    - src/components/CardSearchSection.tsx (renders `data-testid="card-result-cell"` and Load-more control)
    - src/components/CardResultCell.tsx (test id)
    - .planning/phases/02.2-playwright-e2e-harness/02.2-05-SUMMARY.md (standing rule)
    - .planning/phases/02.3-scryfall-api-migration/02.3-RESEARCH.md §Validation Architecture
  </read_first>
  <action>
    Create THREE files. This task executes FIRST — BEFORE any deletion. The pre-deletion codebase state is:
    - `src/lib/scryfall-client.ts` + `.test.ts` still exist (untouched since Phase 02.2).
    - `src/lib/scryfall.ts` + `scryfall-queries.ts` exist (created in plan 02.3-02).
    - Stores + components import from the NEW wrapper (plans 02.3-02 / 02.3-03).
    - `@scryfall/api-types` still in package.json dependencies.
    - `package.json` `build` script is still `vite build` (tsc -b not yet restored).

    **File 1: `e2e/fixtures/searches/card-search-sample.json`** — PAGE 1 stub response (has_more: true, 2 cards):

    ```json
    {
      "object": "list",
      "total_cards": 4,
      "has_more": true,
      "next_page": "https://api.scryfall.com/cards/search?page=2",
      "data": [
        {
          "object": "card",
          "id": "11111111-1111-1111-1111-111111111111",
          "oracle_id": "22222222-2222-2222-2222-222222222222",
          "name": "Sol Ring",
          "layout": "normal",
          "type_line": "Artifact",
          "oracle_text": "{T}: Add {C}{C}.",
          "mana_cost": "{1}",
          "color_identity": [],
          "keywords": [],
          "image_uris": {
            "normal": "https://example.com/sol-ring-normal.jpg",
            "small":  "https://example.com/sol-ring-small.jpg",
            "large":  "https://example.com/sol-ring-large.jpg",
            "art_crop": "https://example.com/sol-ring-art.jpg",
            "border_crop": "https://example.com/sol-ring-border.jpg",
            "png": "https://example.com/sol-ring.png"
          },
          "legalities": { "commander": "legal" }
        },
        {
          "object": "card",
          "id": "33333333-3333-3333-3333-333333333333",
          "oracle_id": "44444444-4444-4444-4444-444444444444",
          "name": "Arcane Signet",
          "layout": "normal",
          "type_line": "Artifact",
          "oracle_text": "{T}: Add one mana of any color in your commander's color identity.",
          "mana_cost": "{2}",
          "color_identity": [],
          "keywords": [],
          "image_uris": {
            "normal": "https://example.com/arcane-signet-normal.jpg",
            "small":  "https://example.com/arcane-signet-small.jpg",
            "large":  "https://example.com/arcane-signet-large.jpg",
            "art_crop": "https://example.com/arcane-signet-art.jpg",
            "border_crop": "https://example.com/arcane-signet-border.jpg",
            "png": "https://example.com/arcane-signet.png"
          },
          "legalities": { "commander": "legal" }
        }
      ]
    }
    ```

    **File 2: `e2e/fixtures/searches/card-search-sample-page2.json`** — PAGE 2 stub response (has_more: false, 2 DIFFERENT cards to prove grid growth on Load-more click):

    ```json
    {
      "object": "list",
      "total_cards": 4,
      "has_more": false,
      "data": [
        {
          "object": "card",
          "id": "55555555-5555-5555-5555-555555555555",
          "oracle_id": "66666666-6666-6666-6666-666666666666",
          "name": "Command Tower",
          "layout": "normal",
          "type_line": "Land",
          "oracle_text": "{T}: Add one mana of any color in your commander's color identity.",
          "mana_cost": "",
          "color_identity": [],
          "keywords": [],
          "image_uris": {
            "normal": "https://example.com/command-tower-normal.jpg",
            "small":  "https://example.com/command-tower-small.jpg",
            "large":  "https://example.com/command-tower-large.jpg",
            "art_crop": "https://example.com/command-tower-art.jpg",
            "border_crop": "https://example.com/command-tower-border.jpg",
            "png": "https://example.com/command-tower.png"
          },
          "legalities": { "commander": "legal" }
        },
        {
          "object": "card",
          "id": "77777777-7777-7777-7777-777777777777",
          "oracle_id": "88888888-8888-8888-8888-888888888888",
          "name": "Exotic Orchard",
          "layout": "normal",
          "type_line": "Land",
          "oracle_text": "{T}: Add one mana of any color a commander shares with a land an opponent controls.",
          "mana_cost": "",
          "color_identity": [],
          "keywords": [],
          "image_uris": {
            "normal": "https://example.com/exotic-orchard-normal.jpg",
            "small":  "https://example.com/exotic-orchard-small.jpg",
            "large":  "https://example.com/exotic-orchard-large.jpg",
            "art_crop": "https://example.com/exotic-orchard-art.jpg",
            "border_crop": "https://example.com/exotic-orchard-border.jpg",
            "png": "https://example.com/exotic-orchard.png"
          },
          "legalities": { "commander": "legal" }
        }
      ]
    }
    ```

    **File 3: `e2e/specs/07-card-search.spec.ts`** — Playwright spec. Asserts result grid renders, clicks Load-more, asserts grid grows (SRCH-07 E2E coverage per checker W-8).

    Use a stateful route override so the first `/cards/search` request returns page1 and the second returns page2. Example skeleton:

    ```ts
    // e2e/specs/07-card-search.spec.ts
    import { test, expect } from '../helpers/stubScryfall';
    import { installConsoleGate } from '../helpers/consoleGate';
    import { createDeckAndSetCommander } from '../helpers/commanderFlows';
    import cardSearchSample      from '../fixtures/searches/card-search-sample.json'       with { type: 'json' };
    import cardSearchSamplePage2 from '../fixtures/searches/card-search-sample-page2.json' with { type: 'json' };

    test.describe('Phase 02.3 — card search regression (SRCH-01..07)', () => {
      test.beforeEach(async ({ page }) => {
        // Stateful rotation: first /cards/search?id<=... hit → page1; second → page2.
        let searchHits = 0;
        await page.route(/\/cards\/search\?.*id(%3C%3D|<=)/i, async route => {
          searchHits++;
          const body = searchHits === 1 ? cardSearchSample : cardSearchSamplePage2;
          await route.fulfill({ json: body, status: 200 });
        });
      });

      test('renders result grid AND pagination click grows the grid', async ({ page }) => {
        const consoleGate = installConsoleGate(page);
        await createDeckAndSetCommander(page, 'atraxa');

        // Initial grid: expect page1 results (Sol Ring + Arcane Signet).
        const cardCells = page.getByTestId('card-result-cell');
        await expect(cardCells).toHaveCount(2);
        await expect(page.getByText('Sol Ring', { exact: false })).toBeVisible();
        await expect(page.getByText('Arcane Signet', { exact: false })).toBeVisible();

        // Load-more control is visible because has_more: true in page1 fixture.
        const loadMore = page.getByRole('button', { name: /load more/i });
        await expect(loadMore).toBeVisible();

        // SRCH-07 E2E coverage: click it, assert the grid grows.
        await loadMore.click();
        await expect(cardCells).toHaveCount(4);
        await expect(page.getByText('Command Tower', { exact: false })).toBeVisible();
        await expect(page.getByText('Exotic Orchard',  { exact: false })).toBeVisible();

        await consoleGate.assertClean();
      });
    });
    ```

    **Implementation notes:**
    - If `commanderFlows.ts` doesn't expose `createDeckAndSetCommander`, use the helper actually present or inline the steps.
    - If `CardSearchSection` renders the Load-more control differently (link, styled div, specific testid), adjust the selector — the goal is asserting the Load-more UX exists AND clicking it produces more results.
    - `installConsoleGate` is from `e2e/helpers/consoleGate.ts`.
    - If the regex `/cards/search\?.*id(%3C%3D|<=)/i` doesn't match the actual request URL (scryfall-api may encode differently), loosen to `/\/cards\/search\?/i` after inspecting the failing request.

    Run the new spec AGAINST THE PRE-DELETION CODE:
    ```bash
    npx playwright test e2e/specs/07-card-search.spec.ts --reporter=line
    ```
    MUST exit 0. This confirms the new wrapper (+ unchanged UI) correctly renders and paginates stubbed responses before we delete the old client.

    Then run the full Playwright suite to confirm no regression in specs 00-06:
    ```bash
    npm run e2e
    ```
    MUST exit 0.

    **Do NOT delete `scryfall-client.ts` in this task.** Do NOT uninstall `@scryfall/api-types`. Do NOT modify the build script. Those are Task 2.

    **Commit:** After `<verify>` passes, `git add e2e/specs/07-card-search.spec.ts e2e/fixtures/searches/card-search-sample.json e2e/fixtures/searches/card-search-sample-page2.json && git commit -m "test(02.3-05): add card-search Playwright regression with Load-more pagination (SRCH-07 E2E)"`.
  </action>
  <verify>
    <automated>test -f e2e/specs/07-card-search.spec.ts &amp;&amp; test -f e2e/fixtures/searches/card-search-sample.json &amp;&amp; test -f e2e/fixtures/searches/card-search-sample-page2.json &amp;&amp; test -f src/lib/scryfall-client.ts &amp;&amp; grep -F '"@scryfall/api-types"' package.json &amp;&amp; npx playwright test e2e/specs/07-card-search.spec.ts --reporter=line &amp;&amp; npm run e2e</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/specs/07-card-search.spec.ts` exists; imports `test, expect` from `'../helpers/stubScryfall'`
    - `e2e/fixtures/searches/card-search-sample.json` exists with `has_more: true` and 2 distinct card entries
    - `e2e/fixtures/searches/card-search-sample-page2.json` exists with `has_more: false` and 2 DIFFERENT distinct card entries (different ids, different names)
    - Spec includes an assertion targeting `data-testid="card-result-cell"`
    - Spec CLICKS the Load-more button (`.click()` call present on a Load-more-name-matched locator)
    - Spec asserts the grid GROWS post-click (either `toHaveCount(4)` after `toHaveCount(2)`, or new card names become visible)
    - Spec includes an `installConsoleGate` call
    - `src/lib/scryfall-client.ts` STILL EXISTS (pre-deletion state — deletion is Task 2)
    - `grep -F '"@scryfall/api-types"' package.json` returns a match (still installed — removal is Task 2)
    - `npx playwright test e2e/specs/07-card-search.spec.ts` exits 0
    - `npm run e2e` (full suite including specs 00-07) exits 0
  </acceptance_criteria>
  <done>Regression spec + fixtures committed; green against PRE-deletion codebase; baseline established for Task 2's real regression gate</done>
</task>

<task type="auto">
  <name>Task 2: Delete scryfall-client, uninstall @scryfall/api-types, restore `tsc -b && vite build`, re-run full suite</name>
  <files>src/lib/scryfall-client.ts, src/lib/scryfall-client.test.ts, package.json, package-lock.json</files>
  <read_first>
    - src/lib/scryfall-client.ts (confirm it has no remaining importers outside the two files being deleted)
    - src/lib/scryfall-client.test.ts
    - package.json (current `build` script is `vite build`; `@scryfall/api-types` at ^1.0.0-alpha.4)
    - .planning/phases/02.3-scryfall-api-migration/02.3-02-SUMMARY.md (confirms non-UI consumers migrated)
    - .planning/phases/02.3-scryfall-api-migration/02.3-03-SUMMARY.md (confirms components migrated)
    - .planning/phases/02.3-scryfall-api-migration/02.3-04-SUMMARY.md (confirms tsconfig + CI + fixture helper)
  </read_first>
  <action>
    Before deleting, run a final sanity grep to confirm NO production or test file imports from `scryfall-client` anymore:
    ```bash
    grep -rn "scryfall-client" src/ e2e/ --include='*.ts' --include='*.tsx' | grep -v "src/lib/scryfall-client"
    ```
    Expected: empty output.

    If any import still exists outside the two files to delete, STOP and report — plans 02.3-02 or 02.3-03 missed a migration. Fix first in a patch commit before proceeding.

    Assuming the grep is clean, execute in this order:

    1. Delete the old client files:
       ```bash
       rm src/lib/scryfall-client.ts src/lib/scryfall-client.test.ts
       ```

    2. Uninstall the alpha package (mutates `package.json` + `package-lock.json`):
       ```bash
       npm uninstall @scryfall/api-types
       ```

    3. Restore the `build` script in `package.json` (this was deferred from plan 02.3-04 per checker W-2 — only now can `tsc -b` succeed because `src/lib/scryfall-client.ts` with its `@scryfall/api-types` import is gone from `tsconfig.app.json`'s `include: ["src"]` scope):

       Edit `package.json` — change:
       ```json
       "build": "vite build",
       ```
       to:
       ```json
       "build": "tsc -b && vite build",
       ```

    4. Run the full green-suite sweep:
       ```bash
       npm run typecheck &amp;&amp; \
       npm run typecheck:e2e &amp;&amp; \
       npm test &amp;&amp; \
       npm run build &amp;&amp; \
       npm run e2e
       ```
       Every step MUST exit 0. If `tsc -b` errors pointing at a file that still imports `@scryfall/api-types`, that file was missed earlier — fix with a mechanical swap to `import type { Card } from './scryfall'` (or appropriate relative path) and re-run. If a unit test fails, investigate before proceeding.

       **The regression gate (per memory `feedback_test_first_workflow`):** `npm run e2e` MUST pass — specifically `e2e/specs/07-card-search.spec.ts` must STILL be green post-deletion. If it regresses, the new wrapper is missing a case the old client covered; revert the deletion, diagnose, and re-attempt.

    5. Spot-check that zod is still present (used by `src/lib/scryfall.ts`):
       ```bash
       grep -F '"zod"' package.json
       ```
       MUST return a match — zod is NOT removed.

    **Commit:** After `<verify>` passes, `git add -A && git commit -m "chore(02.3-05): delete scryfall-client.ts, uninstall @scryfall/api-types, restore tsc -b && vite build"`. (Use `-A` here because the deletion changes tracked files.)
  </action>
  <verify>
    <automated>test ! -e src/lib/scryfall-client.ts &amp;&amp; test ! -e src/lib/scryfall-client.test.ts &amp;&amp; ! grep -F '"@scryfall/api-types"' package.json &amp;&amp; ! grep -F '"@scryfall/api-types"' package-lock.json &amp;&amp; grep -F '"zod"' package.json &amp;&amp; grep -F '"build": "tsc -b &amp;&amp; vite build"' package.json &amp;&amp; ! grep -rn "scryfall-client" src/ e2e/ &amp;&amp; ! grep -rn "@scryfall/api-types\|ScryfallCard" src/ e2e/ &amp;&amp; npm run typecheck &amp;&amp; npm run typecheck:e2e &amp;&amp; npm test &amp;&amp; npm run build &amp;&amp; npm run e2e</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/scryfall-client.ts` does NOT exist
    - `src/lib/scryfall-client.test.ts` does NOT exist
    - `grep -F "@scryfall/api-types" package.json` returns NOTHING
    - `grep -F "@scryfall/api-types" package-lock.json` returns NOTHING
    - `grep -F '"zod"' package.json` returns a match
    - `grep -F '"build": "tsc -b && vite build"' package.json` returns a match (build script restored)
    - `grep -rn "scryfall-client" src/ e2e/` returns NOTHING
    - `grep -rn "@scryfall/api-types\|ScryfallCard" src/ e2e/` returns NOTHING
    - `npm run typecheck` exits 0
    - `npm run typecheck:e2e` exits 0
    - `npm test` exits 0 (full unit suite)
    - `npm run build` exits 0 (`tsc -b` + `vite build` both pass)
    - `npm run e2e` exits 0 — the regression spec from Task 1 STILL passes post-deletion (real regression gate)
  </acceptance_criteria>
  <done>Alpha package uninstalled, old client files deleted, build script restored to `tsc -b && vite build`, full suite green post-deletion — regression gate passed</done>
</task>

<task type="auto">
  <name>Task 3: Write Phase 02.3 closeout SUMMARY (02.3-05-SUMMARY.md)</name>
  <files>.planning/phases/02.3-scryfall-api-migration/02.3-05-SUMMARY.md</files>
  <read_first>
    - .planning/phases/02.3-scryfall-api-migration/02.3-01-SUMMARY.md through 02.3-04-SUMMARY.md
    - .planning/phases/02.2-playwright-e2e-harness/02.2-REVIEW.md §WR-01
    - .planning/phases/02.2-playwright-e2e-harness/02.2-05-SUMMARY.md (template reference)
  </read_first>
  <action>
    Re-run the complete verification sweep to capture fresh timing data for the SUMMARY:
    ```bash
    time npm run typecheck
    time npm run typecheck:e2e
    time npm test
    time npm run build
    time npm run e2e
    ```
    Every step MUST exit 0. If any fails, fix before writing the SUMMARY.

    Then write `.planning/phases/02.3-scryfall-api-migration/02.3-05-SUMMARY.md` with this structure:

    ```markdown
    ---
    phase: 02.3-scryfall-api-migration
    plan: 05
    status: complete
    completion_date: 2026-04-12
    ---

    # Phase 02.3 Plan 05 — Closeout SUMMARY

    ## Phase Outcome

    Phase 02.3 complete. Scryfall client successfully migrated from hand-rolled `scryfall-client.ts` + `@scryfall/api-types@1.0.0-alpha.4` to `scryfall-api@^4.0.5`. Phase 02.2 WR-01 fully closed.

    ## Test-First Execution Order

    Per memory `feedback_test_first_workflow`, plan 05 executed in this deliberate order:
    1. Task 1 — Added `e2e/specs/07-card-search.spec.ts` + page1/page2 fixtures; confirmed GREEN against the PRE-deletion codebase (old `scryfall-client.ts` still present alongside new `scryfall.ts` wrapper).
    2. Task 2 — Deleted `scryfall-client.ts` + `.test.ts`, uninstalled `@scryfall/api-types`, restored `tsc -b && vite build`; re-ran the full suite. The regression spec from Task 1 STILL passes — that's the real regression gate.
    3. Task 3 — This SUMMARY.

    ## Deliverables

    | Deliverable | Status | Evidence |
    |---|---|---|
    | scryfall-api@^4.0.5 installed | OK | plan 02.3-01 |
    | @scryfall/api-types removed | OK | Task 2 |
    | src/lib/scryfall-client.ts deleted | OK | Task 2 |
    | src/lib/scryfall-client.test.ts deleted | OK | Task 2 |
    | src/lib/scryfall.ts created (thin wrapper) | OK | plan 02.3-02 |
    | src/lib/scryfall-queries.ts created | OK | plan 02.3-02 |
    | Build script restored to `tsc -b && vite build` | OK | Task 2 (deferred from plan 02.3-04 per correctness) |
    | tsconfig.e2e.json overrides removed | OK | plan 02.3-04 |
    | CI typecheck step added to e2e.yml | OK | plan 02.3-04 |
    | e2e/specs/07-card-search.spec.ts added with Load-more click | OK | Task 1 |
    | `as unknown as ScryfallCard.Any` casts eliminated | OK | plans 02.3-03 + 02.3-04 |
    | stubScryfall.ts single `asCard` helper (DRY) | OK | plan 02.3-04 |

    ## WR-01 Closure

    Phase 02.2 REVIEW.md WR-01 had two halves:
    1. **Root cause:** `@scryfall/api-types` alpha ships raw .ts source → forced `build: "vite build"` (no tsc). **CLOSED** by plan 02.3-05 Task 2 restoring `tsc -b && vite build` AFTER `scryfall-client.ts` deletion (the structural prerequisite per `tsconfig.app.json` include: ["src"]).
    2. **CI gap:** Missing typecheck step in `.github/workflows/e2e.yml`. **CLOSED** by plan 02.3-04 Task 3.

    ## Full-Suite Verification

    Executed on {date}:

    | Command | Exit Code | Duration |
    |---|---|---|
    | `npm run typecheck` | 0 | {Xs} |
    | `npm run typecheck:e2e` | 0 | {Xs} |
    | `npm test` | 0 | {Xs} |
    | `npm run build` | 0 | {Xs} |
    | `npm run e2e` | 0 | {Xs} |

    ## Regression Coverage

    - **SRCH-01/02/03/04:** card-search-store.test.ts + new e2e/specs/07-card-search.spec.ts
    - **SRCH-05:** scryfall.test.ts abortable() tests + card-search-store abort test
    - **SRCH-06:** library module-global 100ms throttle (verified live in plan 02.3-01 probe)
    - **SRCH-07:** scryfall.test.ts fetchNextPage test + card-search-store loadMore test + new e2e spec's Load-more CLICK + grid-growth assertion (page1 → page2 fixture rotation)
    - **CMDR-01/02/03:** commander-store.test.ts + existing e2e specs 01-06
    - **CMDR-04/05:** partner-detection.test.ts + CommanderSearch.test.tsx + existing e2e specs 01-06
    - **TEST-01:** new e2e spec satisfies Phase 02.2-05 standing rule

    ## Decisions Verified Against Live Scryfall (from 02.3-01 probe)

    {Summarize the 02.3-01 probe output — 1-2 lines per D-06/D-07/D-08/D-09/D-10.}

    ## Deferred / Out of Scope (unchanged from CONTEXT.md)

    - `released_at: Date` auto-parsing in scryfall-api → Phase 3 BUILD-08 concern.
    - `/cards/collection` batch wrapper → Phase 5 IO-03.
    - Multi-browser E2E → deferred indefinitely.

    ## Next Actions

    Orchestrator spawns reviewer agent (code + test review per memory `feedback_test_first_workflow`). On review green, orchestrator spawns verifier agent. On verifier green, Phase 02.3 is shipped.
    ```

    Fill every `{...}` placeholder with real data from the verification runs. Do NOT commit the file — orchestrator commits after checker passes.
  </action>
  <verify>
    <automated>test -f .planning/phases/02.3-scryfall-api-migration/02.3-05-SUMMARY.md &amp;&amp; grep -E '^status: complete$' .planning/phases/02.3-scryfall-api-migration/02.3-05-SUMMARY.md &amp;&amp; grep -q 'WR-01 Closure' .planning/phases/02.3-scryfall-api-migration/02.3-05-SUMMARY.md &amp;&amp; grep -q 'Test-First Execution Order' .planning/phases/02.3-scryfall-api-migration/02.3-05-SUMMARY.md &amp;&amp; npm run typecheck &amp;&amp; npm run typecheck:e2e &amp;&amp; npm test &amp;&amp; npm run build &amp;&amp; npm run e2e</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/02.3-scryfall-api-migration/02.3-05-SUMMARY.md` exists
    - Frontmatter contains `status: complete`
    - File contains sections `## Test-First Execution Order`, `## Deliverables`, `## WR-01 Closure`, `## Full-Suite Verification`, `## Regression Coverage`
    - WR-01 Closure section documents BOTH halves (root cause + CI gap) closed
    - All five verification commands (typecheck, typecheck:e2e, test, build, e2e) exit 0 at SUMMARY authoring time
    - Does NOT contain `TODO`, `{date}`, or `{Xs}` (every placeholder filled)
  </acceptance_criteria>
  <done>Phase 02.3 verified end-to-end green; closeout SUMMARY authored with evidence for every deliverable + test-first ordering narrative; ready for reviewer + verifier agents</done>
</task>

</tasks>

<verification>
  <automated>
    # Final phase-level gate: every verifiable truth about the phase must hold.
    test ! -e src/lib/scryfall-client.ts &amp;&amp; \
    test ! -e src/lib/scryfall-client.test.ts &amp;&amp; \
    ! grep -F '"@scryfall/api-types"' package.json &amp;&amp; \
    ! grep -rnF "@scryfall/api-types" src/ e2e/ &amp;&amp; \
    ! grep -rnF "ScryfallCard" src/ e2e/ &amp;&amp; \
    test -f e2e/specs/07-card-search.spec.ts &amp;&amp; \
    test -f e2e/fixtures/searches/card-search-sample-page2.json &amp;&amp; \
    grep -F '"build": "tsc -b &amp;&amp; vite build"' package.json &amp;&amp; \
    grep -E "- name: Typecheck" .github/workflows/e2e.yml &amp;&amp; \
    npm run typecheck &amp;&amp; \
    npm run typecheck:e2e &amp;&amp; \
    npm test &amp;&amp; \
    npm run build &amp;&amp; \
    npm run e2e
  </automated>
</verification>

<success_criteria>
1. **Test-first ordering satisfied:** Task 1 added and ran the regression spec against the pre-deletion codebase BEFORE Task 2 deleted anything.
2. `src/lib/scryfall-client.ts` and `src/lib/scryfall-client.test.ts` are deleted (Task 2).
3. `@scryfall/api-types` is removed from `package.json` and `package-lock.json` (Task 2).
4. `package.json` `build` script is restored to `tsc -b && vite build` (Task 2 — not plan 02.3-04, per structural correctness).
5. `grep -rn "@scryfall/api-types\|ScryfallCard" src/ e2e/` returns NOTHING.
6. New Playwright spec `e2e/specs/07-card-search.spec.ts` exists and passes; it CLICKS Load-more and asserts the grid grows (SRCH-07 E2E coverage).
7. Full verification suite (typecheck, typecheck:e2e, unit tests, build, e2e) all exit 0 post-deletion.
8. `02.3-05-SUMMARY.md` documents phase completion with WR-01 closure (both halves) and test-first execution narrative.
</success_criteria>

<output>
The SUMMARY at `.planning/phases/02.3-scryfall-api-migration/02.3-05-SUMMARY.md` is the phase closeout artifact. Orchestrator then:
1. Spawns reviewer agent (code + test review).
2. On review green, spawns verifier agent.
3. On verifier green, commits all Phase 02.3 work and marks phase complete in STATE.md + ROADMAP.md.
</output>
