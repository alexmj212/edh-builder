---
phase: 02.3-scryfall-api-migration
plan: 05
subsystem: delete-regression-build-restore
status: complete
tags: [scryfall, playwright, regression, closeout, wr-01, phase-closeout]

requires:
  - phase: 02.3-04
    provides: "tsconfig.e2e.json alpha overrides removed; stubScryfall.ts migrated to Pick<Card> + asCard helper; CI typecheck step in e2e.yml; tsconfig.app.json exclude bridge for scryfall-client.{ts,test.ts}"
provides:
  - "e2e/specs/07-card-search.spec.ts — Playwright regression covering SRCH-01..07 with Load-more click + grid growth assertion (4 card cells post-click)"
  - "e2e/fixtures/searches/card-search-sample.json — page 1 stub (has_more: true, 2 cards)"
  - "e2e/fixtures/searches/card-search-sample-page2.json — page 2 stub (has_more: false, 2 distinct cards)"
  - "src/lib/scryfall-client.ts DELETED"
  - "src/lib/scryfall-client.test.ts DELETED"
  - "@scryfall/api-types uninstalled from package.json + package-lock.json (4 packages removed)"
  - "package.json build script restored to 'tsc -b && vite build' (deferred from 02.3-04 per checker W-2)"
  - "tsconfig.app.json exclude + TODO(02.3-05) bridge removed (defers to empty — include: ['src'] scope fully re-enabled)"
  - "e2e/README.md fixture-type reference updated from @scryfall/api-types to scryfall-api's Card"
  - "WR-01 fully closed — build-script root cause (tsc -b restored) + CI typecheck step (plan 02.3-04) both in place"
affects:
  - "Phase 02.3 closeout"
  - "v1.0 milestone progress"

tech-stack:
  added: []
  removed:
    - "@scryfall/api-types@^1.0.0-alpha.4"
  patterns:
    - "Test-first regression gate: new E2E spec added + confirmed green against pre-deletion code BEFORE the underlying module was removed, re-run post-deletion as the real regression gate"
    - "Temporary tsconfig exclude as a bridge: 02.3-04 added it, 02.3-05 removed it — TODO(plan-id) comment made the handoff explicit"

key-files:
  created:
    - e2e/specs/07-card-search.spec.ts
    - e2e/fixtures/searches/card-search-sample.json
    - e2e/fixtures/searches/card-search-sample-page2.json
  modified:
    - package.json
    - package-lock.json
    - tsconfig.app.json
    - e2e/README.md
  deleted:
    - src/lib/scryfall-client.ts
    - src/lib/scryfall-client.test.ts

key-decisions:
  - "Test-first ordering (per memory feedback_test_first_workflow): Task 1 added the regression spec FIRST and confirmed green against the pre-deletion codebase; Task 2 deleted the old client and re-ran the spec — passing post-deletion is the real regression gate."
  - "Fixture images (img.example/example.com hosts) intercepted with a 1×1 PNG stub inside the spec's beforeEach — prevents ERR_NAME_NOT_RESOLVED console noise that the consoleGate would otherwise flag. Scoped to this spec (other specs don't install the console gate on commander flows)."
  - "Stateful page-rotation (cardSearchHits counter) rather than URL-pattern-based page2 detection — scryfall-api's MagicPageResult increments an internal ?page= on next(), and matching on the page param would couple the spec to library-internal URL construction. The counter is portable across library updates."
  - "Commander-search vs card-search discriminator: query contains `id<=` iff it's a card-search; q contains `t:legendary` iff it's a commander-search. Spec intercepts only card-searches and lets commander-searches fall through to stubScryfall's existing handlers."

requirements-completed:
  - SRCH-01
  - SRCH-02
  - SRCH-03
  - SRCH-04
  - SRCH-05
  - SRCH-06
  - SRCH-07
  - WR-01

metrics:
  tasks: 3/3
  duration: ~8 min
  completed: 2026-04-12
---

# Phase 02.3 Plan 05 — Closeout SUMMARY

**One-liner:** Added the SRCH-01..07 Playwright regression spec with Load-more click + grid-growth assertion against the pre-deletion code, then deleted `src/lib/scryfall-client.ts` + its test, uninstalled `@scryfall/api-types`, removed plan 02.3-04's `tsconfig.app.json` exclude bridge, and restored the `tsc -b && vite build` script — the regression spec re-runs green post-deletion, which is the real regression gate. Phase 02.3 closed, WR-01 retired in full.

## Phase Outcome

Phase 02.3 is complete. The Scryfall client is successfully migrated from the hand-rolled `src/lib/scryfall-client.ts` + `@scryfall/api-types@1.0.0-alpha.4` to a thin `src/lib/scryfall.ts` wrapper over `scryfall-api@^4.0.5`. Phase 02.2 WR-01 is fully closed (both halves — root cause + CI gap). The codebase now has ONE Scryfall type source.

## Test-First Execution Order

Per memory `feedback_test_first_workflow`, plan 05 executed in this deliberate order:

1. **Task 1 (TEST FIRST — commit `43950ee`):** Added `e2e/specs/07-card-search.spec.ts` + `card-search-sample.json` (page 1) + `card-search-sample-page2.json` (page 2). Spec asserts grid renders from page 1 stub, Load-more button is visible (has_more: true), clicking it grows the grid with page 2 stub (4 `card-result-cell` elements post-click). Confirmed GREEN against the PRE-deletion codebase with old `scryfall-client.ts` still present alongside the new `scryfall.ts` wrapper. Full Playwright suite (8/8) green at baseline.

2. **Task 2 (DELETE + RESTORE BUILD — commit `e087055`):** Deleted `src/lib/scryfall-client.ts` + `scryfall-client.test.ts`. Uninstalled `@scryfall/api-types` (4 packages removed). Removed 02.3-04's `tsconfig.app.json` exclude + `// TODO(02.3-05)` comment. Restored `package.json` `build` script to `tsc -b && vite build`. Updated `e2e/README.md` fixture-type reference. Re-ran full suite including the Task 1 regression spec — **it STILL passes**, which is the real regression gate per memory `feedback_test_first_workflow`.

3. **Task 3 (CLOSEOUT — this SUMMARY):** Re-ran the full verification sweep with timing; authored this document.

## Task Commits

| Task | Commit    | Description                                                                                                 |
| ---- | --------- | ----------------------------------------------------------------------------------------------------------- |
| 1    | `43950ee` | test(02.3-05): add card-search Playwright regression with Load-more pagination (SRCH-07 E2E)                |
| 2    | `e087055` | chore(02.3-05): delete scryfall-client.ts, uninstall @scryfall/api-types, restore tsc -b && vite build      |

## Deliverables

| Deliverable                                       | Status | Evidence                                                  |
| ------------------------------------------------- | ------ | --------------------------------------------------------- |
| `scryfall-api@^4.0.5` installed                   | OK     | plan 02.3-01                                              |
| `@scryfall/api-types` removed                     | OK     | Task 2 — `grep '"@scryfall/api-types"' package*.json` empty |
| `src/lib/scryfall-client.ts` deleted              | OK     | Task 2 — file absent                                      |
| `src/lib/scryfall-client.test.ts` deleted         | OK     | Task 2 — file absent                                      |
| `src/lib/scryfall.ts` created (thin wrapper)      | OK     | plan 02.3-02                                              |
| `src/lib/scryfall-queries.ts` created             | OK     | plan 02.3-02                                              |
| Build script restored to `tsc -b && vite build`   | OK     | Task 2 (deferred from 02.3-04 per structural correctness) |
| `tsconfig.app.json` exclude + TODO(02.3-05) gone  | OK     | Task 2                                                    |
| `tsconfig.e2e.json` overrides removed             | OK     | plan 02.3-04                                              |
| CI typecheck step in `.github/workflows/e2e.yml`  | OK     | plan 02.3-04                                              |
| `e2e/specs/07-card-search.spec.ts` with Load-more | OK     | Task 1                                                    |
| `as unknown as ScryfallCard.Any` casts eliminated | OK     | plans 02.3-03 + 02.3-04                                   |
| `stubScryfall.ts` single `asCard` helper (DRY)    | OK     | plan 02.3-04                                              |
| `e2e/README.md` fixture-type reference updated    | OK     | Task 2                                                    |

## WR-01 Closure

Phase 02.2 REVIEW.md's WR-01 had two halves:

1. **Root cause:** `@scryfall/api-types` alpha shipped raw `.ts` source that conflicted with our `verbatimModuleSyntax + erasableSyntaxOnly` strict flags, forcing `build: "vite build"` (no `tsc -b`). **CLOSED** by plan 02.3-05 Task 2 — deleting `scryfall-client.ts` drops `@scryfall/api-types` out of the `tsconfig.app.json include: ["src"]` project graph, and the uninstall removes it from `node_modules` entirely. With the structural prerequisite satisfied, the build script is restored to `tsc -b && vite build`.

2. **CI gap:** `.github/workflows/e2e.yml` lacked a typecheck step before Playwright. **CLOSED** by plan 02.3-04 Task 3 — `Typecheck (src + e2e)` step now runs between `npm ci` and `npx playwright install`, gating every PR on type correctness before browser download.

Both halves closed. WR-01 retired.

## Full-Suite Verification

Executed at SUMMARY authoring time (2026-04-12):

| Command                | Exit Code | Duration | Notes                                                                 |
| ---------------------- | --------- | -------- | --------------------------------------------------------------------- |
| `npm run typecheck`    | 0         | 2.2s     | `tsc -b` clean against full `src/` (no more exclude bridge)           |
| `npm run typecheck:e2e`| 0         | 1.3s     | `tsc -p tsconfig.e2e.json` clean; fixture-helper types resolve        |
| `npm test`             | 0         | 3.0s     | 170 unit tests passed (19 files) — 19 dropped with `scryfall-client.test.ts` deletion (was 189) |
| `npm run build`        | 0         | 5.7s     | `tsc -b` + `vite build`; 60 modules → 418 KB JS / 22 KB CSS bundle    |
| `npm run e2e`          | 0         | 6.8s     | 8/8 Playwright specs including new `07-card-search.spec.ts` — real regression gate PASSED |

All five gates green.

## Regression Coverage

Maps every phase requirement (and WR-01) to its test source post-plan:

- **SRCH-01 (card search renders a grid):** `src/store/card-search-store.test.ts` (unit) + `e2e/specs/07-card-search.spec.ts` (E2E asserts `card-result-cell` count post-query).
- **SRCH-02 (query filter composition):** `src/lib/scryfall-queries.test.ts` (unit tests for `buildSearchQuery`). `07-card-search.spec.ts` indirectly exercises it because the spec's stub route fires only when the query contains `id<=` — proving the builder's identity clause is present.
- **SRCH-03 (debounced filter input):** `src/hooks/useDebouncedValue.test.ts` + `card-search-store.test.ts` search call-pattern tests.
- **SRCH-04 (stale-request abort):** `src/lib/scryfall.test.ts` (abortable() tests) + `card-search-store.test.ts` abort test.
- **SRCH-05 (Scryfall error handling):** `src/lib/scryfall.test.ts` 404/error tests + `card-search-store.test.ts` error-state test.
- **SRCH-06 (100ms shared throttle):** Library module-global hard-coded 100ms throttle — verified LIVE in plan 02.3-01 probe against `api.scryfall.com` (sequential `Cards.byId` gap = 100ms).
- **SRCH-07 (pagination — Load more):** `src/lib/scryfall.test.ts` `fetchNextPage` unit + `card-search-store.test.ts` `loadMore` unit + **new `07-card-search.spec.ts` E2E Load-more click with grid-growth assertion (2 → 4 card cells, with 4 distinct card names visible)**.
- **CMDR-01/02/03 (commander selection / partner slot / identity combine):** `src/store/commander-store.test.ts` + existing e2e specs 01-06.
- **CMDR-04/05 (partner detection + rules):** `src/lib/partner-detection.test.ts` + `src/components/CommanderSearch.test.tsx` + existing e2e specs 01-06.
- **WR-01:** Closed — see §WR-01 Closure.

Per Phase 02.2-05 standing rule (every phase ships with E2E coverage of its user-facing flows), `e2e/specs/07-card-search.spec.ts` is the phase 02.3 contribution.

## Decisions Verified Against Live Scryfall (from 02.3-01 probe)

- **D-06 (Vite/ESM compat):** PASS — `vite build` green, ESM import resolves.
- **D-07 (Card field parity):** PASS — all 10 required fields present on sample response.
- **D-08 (AbortSignal path):** PASS — **D-04 option 3 (promise-race wrapper) ratified**; AbortError raised on cancel.
- **D-09 (shared 100ms throttle):** PASS — library ships a module-global hard-coded 100ms throttle; sequential `Cards.byId` gap = 100ms.
- **D-10 (query-builder coverage):** ACCEPTED GAP — library ships zero query builders; all three stay in `src/lib/scryfall-queries.ts` (D-03 fallback). Existing `buildCommanderSearchQuery` output validated against live API (2926 hits); named-partner query (`!"Tymna the Weaver"`) returned exactly 1 card.

## Deviations from Plan

**None from Task 1 or Task 2's written scope.** Plan executed exactly as specified, including the mandatory cleanup from prior plan (tsconfig.app.json exclude + TODO removal) that plan 02.3-04's SUMMARY flagged as required.

**Rule 2 (auto-add missing critical functionality) applied once, inline, in Task 1:** The new spec needed an image-route block (`img.example` / `example.com` → 1×1 PNG) inside `beforeEach` so `installConsoleGate` wouldn't flag `ERR_NAME_NOT_RESOLVED` noise from the existing fixture JSON files. Not plan-visible because other specs don't install the console gate on commander-selection flows, so the gap only surfaced when spec 07 did. Scoped to this spec's beforeEach (not pushed into `stubScryfall.ts`) so the existing 6 partner specs keep their simpler stub surface.

**Rule 1 (auto-fix bug) applied once, inline, in Task 2:** `e2e/README.md:13` still said "typed via @scryfall/api-types" — stale after the migration. Fixed in the same commit as the deletion because it's a one-line doc drift from the same root cause.

Both inline fixes caught by the post-deletion grep acceptance check (`grep -rn "@scryfall/api-types" src/ e2e/` must be empty — the README line would have failed that).

## Issues Encountered

- **Console noise from fixture image URLs:** First run of spec 07 failed not on card assertions (which all passed) but on `installConsoleGate` flagging `ERR_NAME_NOT_RESOLVED` for `img.example` atraxa image URLs. Root cause: card-search E2E is the first spec where the user's flow both (a) picks a commander (which mounts `CommanderPanel` with an `<img src={image_uris.normal}>`) AND (b) asserts clean console output. Fix: scoped image-route block in the spec's beforeEach. No library-wide change needed.
- **None semantic.** The regression gate (Task 1 spec re-running post-deletion) passed first try — confirming the `scryfall.ts` wrapper's behavior matches the old `scryfall-client.ts` for the card-search flow.

## Deferred / Out of Scope (unchanged from CONTEXT.md)

- `released_at: Date` auto-parsing in `scryfall-api` → Phase 3 BUILD-08 concern.
- `/cards/collection` batch wrapper → Phase 5 IO-03.
- Multi-browser E2E → deferred indefinitely.

## Next Actions

Phase 02.3 is shipped from an execution standpoint. The orchestrator next:

1. Spawns reviewer agent for code + test review per memory `feedback_test_first_workflow`.
2. On review green, spawns verifier agent (full-suite re-run + requirement audit).
3. On verifier green, the phase is marked complete in STATE.md + ROADMAP.md.

---

*Phase: 02.3-scryfall-api-migration*
*Completed: 2026-04-12*

## Self-Check: PASSED

- FOUND: `e2e/specs/07-card-search.spec.ts`
- FOUND: `e2e/fixtures/searches/card-search-sample.json`
- FOUND: `e2e/fixtures/searches/card-search-sample-page2.json`
- MISSING (as required): `src/lib/scryfall-client.ts`
- MISSING (as required): `src/lib/scryfall-client.test.ts`
- FOUND commit: `43950ee` (Task 1 — regression spec + fixtures)
- FOUND commit: `e087055` (Task 2 — deletion + uninstall + build-script restore + tsconfig cleanup)
- VERIFIED: `npm run typecheck` exits 0
- VERIFIED: `npm run typecheck:e2e` exits 0
- VERIFIED: `npm test` 170/170 green
- VERIFIED: `npm run build` (`tsc -b` + `vite build`) exits 0
- VERIFIED: `npm run e2e` 8/8 green — `07-card-search.spec.ts` passes post-deletion (real regression gate)
- VERIFIED: `grep -F '"@scryfall/api-types"' package.json package-lock.json` returns nothing
- VERIFIED: `grep -rn "scryfall-client\|@scryfall/api-types\|ScryfallCard" src/ e2e/` returns nothing
- VERIFIED: `grep -F "TODO(02.3-05)" tsconfig.app.json` returns nothing
- VERIFIED: `grep -F '"build": "tsc -b && vite build"' package.json` returns a match
