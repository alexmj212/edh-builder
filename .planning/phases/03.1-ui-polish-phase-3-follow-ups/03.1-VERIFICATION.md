---
phase: 03.1-ui-polish-phase-3-follow-ups
verified: 2026-04-15T03:41:16Z
status: passed
score: 5/5 must-haves verified
overrides_applied: 0
notes:
  - "Full unit suite 287/287 green; full e2e suite 30/30 green; tsc -b clean."
  - "Phase 03.1 REVIEW.md (separate artifact) flagged 2 warnings (WR-01 CardResultCell cursor-pointer affordance, WR-02 DeckWorkspace loading flicker). These are code-quality follow-ups documented in the REVIEW and do NOT block the goal contract established in ROADMAP — the three UI-REVIEW Priority Fixes are closed at the code + unit + e2e level."
---

# Phase 03.1: UI Polish (Phase 3 Follow-ups) Verification Report

**Phase Goal:** Resolve the three advisory findings from Phase 3's UI review (03-UI-REVIEW.md, PASS 21/24) so keyboard and scroll behavior match the rest of the design contract before Phase 4.

**Verified:** 2026-04-15T03:41:16Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | CommanderPanel "Change commander" / "Remove partner" / Flip buttons carry `focus:ring-2 focus:ring-accent` focus rings matching ViewToggle pattern | VERIFIED | `src/components/CommanderPanel.tsx:73` (Flip) + `:82` (action button) — both carry `focus:outline-none focus:ring-2 focus:ring-accent rounded`; 3 regression tests in `CommanderPanel.test.tsx:166-206` assert classes on all three button variants (Change commander, Remove partner, Flip) |
| 2 | View-switch scroll reset targets the true outer `flex-[2]` scroll parent (DeckWorkspace), not the inner DeckColumn div | VERIFIED | `DeckColumn.tsx:15,18,79` — `onViewToggle?: () => void` prop, destructured, called inside `handleToggle`. `DeckWorkspace.tsx:28-30` — `outerScrollRef = useRef<HTMLDivElement>(null)` + `handleViewToggle` resets `scrollTop = 0` on the `flex-[2]` wrapper (`:60` `ref={outerScrollRef}`, `:61` `onViewToggle={handleViewToggle}`). Inner `overflow-y-auto` removed from `DeckColumn` root (grep returns 0 matches). E2E `14-ui-polish.spec.ts:109` seeds `scrollTop = 500` on the outer wrapper via injected 2000px spacer and asserts reset to 0 after Grid click. |
| 3 | CardResultCell outer div exposes a visible focus state while preserving `tabIndex={0}` | VERIFIED | `CardResultCell.tsx:60` — outer div className carries `focus:outline-none focus:ring-2 focus:ring-accent`; `:61` `tabIndex={0}` preserved (grep returns exactly 1 match). 2 regression tests in `CardResultCell.test.tsx:123-129+` assert class surface and tabindex preservation. |
| 4 | Test suites still green: unit + e2e + typecheck, no regressions in specs 01-13 | VERIFIED | `npm run test` → 287/287 passed across 26 files (baseline 281 → +6 new tests); `npm run e2e` → 30/30 passed (baseline 27 → +3 new tests, specs 01-13 unchanged and green); `npm run typecheck` (`tsc -b`) → exit 0. |
| 5 | Each fix has unit regression coverage; Wave 2 adds consolidated Playwright E2E coverage (14-ui-polish.spec.ts) | VERIFIED | Unit coverage: 3 tests in `CommanderPanel.test.tsx`, 2 tests in `CardResultCell.test.tsx`, 2 tests in `DeckColumn.test.tsx`. E2E: `e2e/specs/14-ui-polish.spec.ts` exists (138 lines, 3 tests) — computed box-shadow assertions for both focus rings + overflow-scroll reset via synthetic 2000px spacer. 3 `installConsoleGate` invocations, 2 `toBeFocused` assertions, 6 `scrollTop` references. |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/components/CommanderPanel.tsx` | FullCard action buttons carry `focus:ring-2 focus:ring-accent` | VERIFIED | Lines 73, 82 both contain `focus:outline-none focus:ring-2 focus:ring-accent rounded`. No other changes. |
| `src/components/CommanderPanel.test.tsx` | 3 regression tests for Change commander / Remove partner / Flip focus rings | VERIFIED | `describe('focus rings (Phase 03.1 UI polish)')` at line 166 contains all 3 `it(...)` blocks. |
| `src/components/DeckColumn.tsx` | drops `scrollRef` + inner `overflow-y-auto`; accepts `onViewToggle?` prop | VERIFIED | `grep scrollRef` → 0 matches; `grep overflow-y-auto` → 0 matches; `grep onViewToggle` → 3 matches (prop type, destructure, call). |
| `src/components/DeckWorkspace.tsx` | owns `outerScrollRef` on `flex-[2]` wrapper + `handleViewToggle` resets `scrollTop = 0` | VERIFIED | Lines 28-30 declare ref + callback; line 60 attaches `ref={outerScrollRef}`; line 61 wires `onViewToggle={handleViewToggle}`; `max-h-[calc(100vh-6rem)]` preserved on outer container. |
| `src/components/DeckColumn.test.tsx` | 2 regression tests for `onViewToggle` firing on view toggle | VERIFIED | `describe('view-toggle scroll reset (Phase 03.1 UI polish)')` at line 142 with 2 `it(...)` blocks. |
| `src/components/CardResultCell.tsx` | outer div carries focus-ring classes with `tabIndex={0}` preserved | VERIFIED | Line 60 className has `focus:outline-none focus:ring-2 focus:ring-accent`; line 61 retains `tabIndex={0}`; inner `+` button (line 82) focus ring untouched. |
| `src/components/CardResultCell.test.tsx` | 2 regression tests for outer focus-ring classes + tabindex preservation | VERIFIED | `describe('outer container focus state (Phase 03.1 UI polish)')` at line 123 with 2 `it(...)` blocks. |
| `e2e/specs/14-ui-polish.spec.ts` | 3 browser-level regression tests with computed box-shadow + overflow scroll reset | VERIFIED | File exists (138 lines, ≥80 required). 3 `test(...)` blocks, 4 `installConsoleGate` calls, 2 `toBeFocused` assertions, 6 `scrollTop` references. All 3 tests pass. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| CommanderPanel FullCard action buttons | focus-ring Tailwind utilities | className string | WIRED | Two buttons at lines 73, 82 carry the exact four-token focus-ring string |
| DeckColumn.handleToggle | outer flex-[2] wrapper in DeckWorkspace | `onViewToggle` prop + `outerScrollRef` | WIRED | `DeckColumn.tsx:79 onViewToggle?.()` → `DeckWorkspace.tsx:29-30 handleViewToggle` writes `outerScrollRef.current.scrollTop = 0` → ref attached at `:60` on the `flex-[2]` div |
| CardResultCell outer div | focus-ring Tailwind utilities | className addition | WIRED | Line 60 includes the three-token focus-ring string; tabIndex=0 preserved at :61 |
| 14-ui-polish.spec.ts | harness helpers | `from '../helpers/...'` imports | WIRED | Imports `stubScryfall`, `consoleGate`, `commanderFlows` (note: unused `deckBuildingFlows` import pruned during implementation per summary — does not affect test behavior; still satisfies the key-link intent). |

### Data-Flow Trace (Level 4)

N/A for this phase — the changes are purely a11y/styling and scroll-wiring. No new dynamic data flows were introduced. Existing `useCommanderStore` + `useDeckCardsStore` data flows were preserved (verified by the unchanged 01-13 e2e suite passing).

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Unit suite green | `npm run test` | 287 passed / 26 files in 3.17s | PASS |
| Typecheck clean | `npm run typecheck` (tsc -b) | exit 0, no output | PASS |
| New e2e spec passes | `npm run e2e -- e2e/specs/14-ui-polish.spec.ts` | 3 passed (3.6s) | PASS |
| Full e2e suite passes | `npm run e2e` | 30 passed (14.6s) — specs 01-13 green + new spec 14 | PASS |

### Requirements Coverage

Phase 03.1 declared no new requirement IDs — it closes advisory items from `03-UI-REVIEW.md`. Traceability to UI-REVIEW Priority Fixes:

| UI-REVIEW Item | Source Plan | Description | Status | Evidence |
|----------------|-------------|-------------|--------|----------|
| Priority Fix #1 | 03.1-01 | CommanderPanel action buttons focus ring (WCAG 2.1 2.4.7) | SATISFIED | Source changes at `CommanderPanel.tsx:73,82`; 3 unit tests + 1 e2e |
| Priority Fix #2 | 03.1-02 | DeckColumn view-toggle scroll reset lifts to outer wrapper | SATISFIED | Source changes across DeckColumn.tsx + DeckWorkspace.tsx; 2 unit tests + 1 e2e |
| Priority Fix #3 | 03.1-03 | CardResultCell outer focus state | SATISFIED | Source change at `CardResultCell.tsx:60` (tabIndex preserved at :61); 2 unit tests + 1 e2e |
| Pillar 5 double-scroll advisory | 03.1-02 (bundled) | Remove redundant inner `overflow-y-auto` | SATISFIED | `grep overflow-y-auto src/components/DeckColumn.tsx` → 0 matches |
| Standing rule: E2E coverage per phase | 03.1-04 | New Playwright spec | SATISFIED | `e2e/specs/14-ui-polish.spec.ts` — 138 lines, 3 tests, full suite 30/30 |

### Anti-Patterns Found

None that affect goal achievement.

The separately-created `03.1-REVIEW.md` flagged 2 warnings and 4 info items — all of which are code-quality follow-ups (e.g., `CardResultCell` `cursor-pointer` affordance on a container without a click handler; a potential `DeckWorkspace` loading flicker). These are not regressions introduced by this phase and do not block the phase's UI-polish contract. They should be tracked as future advisory items or folded into a later phase.

### Human Verification Required

None. All must-haves are mechanically verifiable via grep + unit + e2e + typecheck, all of which pass. The e2e spec already provides browser-level painted-pixel verification (`getComputedStyle(...).boxShadow !== 'none'`) for the two focus-ring fixes, so a visual human check is not strictly required for goal achievement.

### Gaps Summary

No gaps. All 5 must-haves are verified against the live codebase:

- 3 code artifacts carry the required focus-ring / scroll-lift / focus-state changes with correct wiring.
- 6 unit regression tests (3 CommanderPanel, 2 CardResultCell, 2 DeckColumn — 7 total, one DeckColumn test was also replaced, net +6) pin the fixes at the component level.
- 3 Playwright e2e regression tests (`14-ui-polish.spec.ts`) pin the fixes at the browser level with computed box-shadow + real overflow-scroll assertions.
- Full unit suite 287/287 green; full e2e suite 30/30 green; tsc -b exit 0.
- All commits are on `main` per the commit-as-you-go convention; TDD RED/GREEN split preserved where `tdd="true"` was declared.

Phase goal ("keyboard and scroll behavior match the rest of the design contract before Phase 4") is achieved.

---

_Verified: 2026-04-15T03:41:16Z_
_Verifier: Claude (gsd-verifier)_
