---
phase: 03.1-ui-polish-phase-3-follow-ups
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/CommanderPanel.tsx
  - src/components/CommanderPanel.test.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Keyboard user tabbing to 'Change commander' sees a visible accent ring around the button"
    - "Keyboard user tabbing to 'Remove partner' sees a visible accent ring around the button"
    - "Keyboard user tabbing to 'Flip' (double-faced cards) sees a visible accent ring around the button"
    - "Unit test asserts focus-ring Tailwind classes are present on all three action buttons"
    - "`bun test` passes (no regression in CommanderPanel existing suite)"
  artifacts:
    - path: "src/components/CommanderPanel.tsx"
      provides: "FullCard sub-component action buttons with focus:ring-2 focus:ring-accent classes"
      contains: "focus:ring-2 focus:ring-accent"
    - path: "src/components/CommanderPanel.test.tsx"
      provides: "Regression test asserting focus-ring classes on action buttons"
      contains: "focus:ring-accent"
  key_links:
    - from: "FullCard action <button> elements (lines 70-77 Flip, 79-85 actionLabel)"
      to: "focus-ring Tailwind utilities"
      via: "className string containing 'focus:outline-none focus:ring-2 focus:ring-accent rounded'"
      pattern: "focus:ring-2\\s+focus:ring-accent"
---

<objective>
Fix UI-REVIEW finding #1: the "Change commander" / "Remove partner" text buttons inside `FullCard` have no visible focus ring. Add the standard focus-ring class set already established by `ViewToggle` and the `CardResultCell` (+) button. Cover the Flip button at the same time — it shares the same styling sibling and the same gap.

Purpose: Close WCAG 2.1 2.4.7 failure flagged in 03-UI-REVIEW.md Pillar 2 (Visuals) and Pillar 6 (Experience Design).

Output:
- `src/components/CommanderPanel.tsx` FullCard action buttons carry `focus:outline-none focus:ring-2 focus:ring-accent rounded`
- `src/components/CommanderPanel.test.tsx` gains a regression assertion for the focus-ring classes on rendered action buttons
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-deck-building-card-display/03-UI-REVIEW.md
@src/components/ViewToggle.tsx
@src/components/CommanderPanel.tsx
@src/components/CommanderPanel.test.tsx

<interfaces>
<!-- Reference implementation: ViewToggle.tsx uses these exact classes for all interactive buttons. -->
<!-- `focus:ring-inset` is ViewToggle-specific (segmented control renders flush-inside a border); -->
<!-- for free-standing text buttons in CommanderPanel we use the non-inset variant with `rounded`. -->

From src/components/ViewToggle.tsx (reference focus-ring pattern):
```
focus:outline-none focus:ring-2 focus:ring-accent focus:ring-inset
```

CommanderPanel target classes (non-inset variant, for standalone text buttons):
```
focus:outline-none focus:ring-2 focus:ring-accent rounded
```

From src/components/CommanderPanel.tsx (current FullCard action buttons):
```tsx
// Flip button (lines 70-77) — currently no focus ring
<button
  type="button"
  onClick={() => setFaceIndex(i => (i === 0 ? 1 : 0))}
  className="text-sm text-text-secondary hover:text-text-primary"
  aria-label="Flip card to other face"
>
  Flip
</button>

// Action button (lines 79-85) — currently no focus ring
<button
  type="button"
  onClick={onAction}
  className="text-sm text-text-secondary hover:text-text-primary"
>
  {actionLabel}
</button>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add focus-ring classes to FullCard action buttons + regression test</name>
  <read_first>
    - src/components/CommanderPanel.tsx (see current FullCard className on buttons at lines 70-77 and 79-85)
    - src/components/ViewToggle.tsx (reference focus-ring pattern — lines 7-8)
    - src/components/CommanderPanel.test.tsx (existing test structure and render helpers — 197 lines)
    - .planning/phases/03-deck-building-card-display/03-UI-REVIEW.md (Priority Fix #1, lines 44-48)
  </read_first>
  <behavior>
    - Regression test: rendering CommanderPanel with a primary commander that is NOT double-faced → the "Change commander" button's className contains `focus:outline-none`, `focus:ring-2`, `focus:ring-accent`, and `rounded`.
    - Regression test: rendering CommanderPanel with a Partner-keyword primary commander + selected partner → the "Remove partner" button's className contains the same four focus-ring tokens.
    - Regression test: rendering CommanderPanel with a double-faced primary commander → the "Flip" button's className contains the same four focus-ring tokens.
    - All 3 assertions use `.toMatch(/focus:ring-2/)` + `.toMatch(/focus:ring-accent/)` on `button.className` (string match, same pattern as ViewToggle.test.tsx lines 56-61).
    - All pre-existing CommanderPanel tests still pass (no behavioral change to the component — only class-string additions).
  </behavior>
  <action>
    1. Open `src/components/CommanderPanel.tsx`. In the `FullCard` sub-component, update the Flip button className (currently line 73): change from `"text-sm text-text-secondary hover:text-text-primary"` to `"text-sm text-text-secondary hover:text-text-primary focus:outline-none focus:ring-2 focus:ring-accent rounded"`.
    2. In the same `FullCard` sub-component, update the action button className (currently line 82): change from `"text-sm text-text-secondary hover:text-text-primary"` to `"text-sm text-text-secondary hover:text-text-primary focus:outline-none focus:ring-2 focus:ring-accent rounded"`.
    3. Do NOT touch any other classes, props, behavior, aria labels, or the EmptyArt / CommanderPanel wrapper.
    4. Open `src/components/CommanderPanel.test.tsx`. Inside the existing `describe('CommanderPanel', ...)` block, add a new nested `describe('focus rings (Phase 03.1 UI polish)', ...)` block with THREE tests:
       - `'Change commander button has focus:ring-2 focus:ring-accent classes'` — seed commander-store with a non-DFC primary commander (use `fakeCard({ keywords: [] })` helper already present in the file, or mirror whatever fake-commander helper this file uses), render CommanderPanel, grab the button via `screen.getByRole('button', { name: 'Change commander' })`, and assert `.className` matches both `/focus:ring-2/` and `/focus:ring-accent/` AND `/focus:outline-none/` AND `/\brounded\b/`.
       - `'Remove partner button has focus:ring-2 focus:ring-accent classes'` — seed a Partner-keyword primary + partner commander, assert same four patterns on the `Remove partner` button.
       - `'Flip button has focus:ring-2 focus:ring-accent classes when commander is double-faced'` — seed a DFC primary (card with `card_faces: [{image_uris:{normal:'...'}}, {image_uris:{normal:'...'}}]`), assert same four patterns on the `Flip` button.
    5. Wrap each test's render in `<StrictMode>` per project convention (match the wrapper style used elsewhere in this test file — if the file already has a `renderCommanderPanel` helper that wraps in StrictMode, reuse it; otherwise wrap inline).
    6. Use `test.each` / parameterized style only if it doesn't obscure which assertion failed; otherwise keep three independent `it(...)` blocks.
  </action>
  <verify>
    <automated>bun run test src/components/CommanderPanel.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'focus:ring-2 focus:ring-accent' src/components/CommanderPanel.tsx` returns >= 2 (Flip button + action button both carry the classes; Flip is only rendered for DFC but the class is still in source).
    - `grep -c 'focus:outline-none' src/components/CommanderPanel.tsx` returns >= 2.
    - `grep 'focus:ring-2' src/components/CommanderPanel.tsx` shows the classes appear on the `Flip` button (containing `aria-label="Flip card to other face"` nearby) AND on the action button (containing `{actionLabel}` nearby).
    - `bun run test src/components/CommanderPanel.test.tsx` exits 0 AND contains at least 3 new passing tests whose names include "focus:ring-2" or "focus rings".
    - `bun test` (full unit suite) exits 0 with test count >= 281 (matches pre-phase-03.1 baseline of 281 + at least 3 new tests).
    - `git diff src/components/CommanderPanel.tsx` shows ONLY className string additions on the two button elements inside `FullCard`; no other lines changed.
  </acceptance_criteria>
  <done>
    CommanderPanel's FullCard Flip, Change commander, and Remove partner buttons all carry `focus:outline-none focus:ring-2 focus:ring-accent rounded`; new regression tests in CommanderPanel.test.tsx assert the classes are present for all three; full unit suite is green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Atomic commit — CommanderPanel focus rings + tests</name>
  <read_first>
    - git status (working tree should show only CommanderPanel.tsx and CommanderPanel.test.tsx modified)
    - ./CLAUDE.md (commit-early-and-often convention)
  </read_first>
  <action>
    Run `git status` and confirm only `src/components/CommanderPanel.tsx` + `src/components/CommanderPanel.test.tsx` are modified (plus this PLAN.md if newly created). Stage only the two component files. Commit with message:
    ```
    fix(03.1): add focus rings to CommanderPanel action buttons

    Closes UI-REVIEW Priority Fix #1 — WCAG 2.1 2.4.7 compliance for
    keyboard users. Mirrors the focus-ring pattern from ViewToggle.

    - FullCard Flip button: focus:outline-none focus:ring-2 focus:ring-accent rounded
    - FullCard action button (Change commander / Remove partner): same classes
    - CommanderPanel.test.tsx: 3 new regression tests asserting classes
    ```
  </action>
  <verify>
    <automated>git log -1 --format=%s | grep -q "focus rings to CommanderPanel"</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --format=%s` output contains "focus rings to CommanderPanel" or equivalent summary.
    - `git log -1 --name-only` lists exactly `src/components/CommanderPanel.tsx` and `src/components/CommanderPanel.test.tsx` (no other files).
    - `git status --porcelain` shows no unstaged changes for these two files.
  </acceptance_criteria>
  <done>
    Commit created on main with the CommanderPanel fix + tests as a single atomic unit per project commit-as-you-go convention.
  </done>
</task>

</tasks>

<verification>
- `bun run test src/components/CommanderPanel.test.tsx` exits 0 with >= 3 new passing tests.
- `bun test` full suite exits 0 with test count >= 284 (281 baseline + 3 new).
- `grep -c 'focus:ring-2 focus:ring-accent' src/components/CommanderPanel.tsx` returns >= 2.
- Typecheck (`bun run typecheck` or `tsc -b`) exits 0.
- Single clean commit on main touching only the two targeted files.
</verification>

<success_criteria>
- [ ] FullCard Flip button has `focus:outline-none focus:ring-2 focus:ring-accent rounded` in source.
- [ ] FullCard action button (Change commander / Remove partner) has the same four classes in source.
- [ ] 3 new regression tests in CommanderPanel.test.tsx assert the classes; all pass under StrictMode.
- [ ] Full unit suite green (no regressions in existing CommanderPanel tests).
- [ ] Change committed atomically with descriptive message referencing UI-REVIEW Fix #1.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-ui-polish-phase-3-follow-ups/03.1-01-SUMMARY.md`.
</output>
