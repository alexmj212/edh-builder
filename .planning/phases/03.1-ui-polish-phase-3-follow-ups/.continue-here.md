---
context: phase
phase: 03.1-ui-polish-phase-3-follow-ups
task: 0
total_tasks: 0
status: not_started
last_updated: 2026-04-15T02:50:35.894Z
---

## Critical Anti-Patterns

_None discovered this session. Phase 3 shipped cleanly; no blocking constraints._

<current_state>
Phase 03 (Deck Building & Card Display) is complete and verified. Phase 03.1 has just been inserted into the roadmap with a customized goal + success criteria targeting the 3 advisory findings from 03-UI-REVIEW.md. No plans generated yet — next action is /gsd-plan-phase 03.1.

Git is clean on main (last commit: 5e24fc1 "docs(roadmap): insert Phase 03.1 polish"). Tests: 281/281 unit + 27/27 e2e + typecheck clean at the moment of pause.
</current_state>

<completed_work>

Phase 03 shipped end-to-end this session (single-phase run via `/gsd-autonomous --only 3`):
- Wave 1 (03-01): Dexie v4 additive migration + extended Deck/DeckCard types + 16 scaffolded files + live Scryfall oracle_id probe. `oracleid:` operator locked (both `oracleid:` and `oracle_id:` returned 127 prints; `oracleid:` matches Scryfall's own `prints_search_uri`). Human-action checkpoint approved by user. Plan's hardcoded Sol Ring oracle_id (`1e14d5f3-…`) was wrong — re-resolved to `6ad8011d-3471-4369-9d68-b264cc027487`.
- Wave 2 (03-02): `isBasicLand` (12-name whitelist + snow-land regex) + `categorizeCard` (Land-before-Creature precedence). 35 tests, BUILD-04 + BUILD-06 closed.
- Wave 3 (03-03): Zustand `deck-cards-store` with atomic 3-table Dexie transactions (`deckCards` + `deckChanges` + `decks.updatedAt`). `resolveOriginalReleaseDate` uses `oracleid:` + `unique:'prints'/order:'released'/dir:'asc'` + cross-deck dedupe + YYYY-MM-DD + null-on-failure. 16 tests, BUILD-01/02/03/07/08 + DECK-09 closed.
- Wave 4 (03-04): `ViewToggle`, `DeckListView`, `DeckGridView` — all pure (no store imports). 28 tests. UI-02 closed.
- Wave 5 (03-05): Integration — CardResultCell `(+)` wired, DeckWorkspace 60/40 split with CommanderPanel art_crop, Playwright spec 13-deck-building.spec.ts with 12 new tests. Incidentally fixed 14 pre-existing e2e failures by moving CommanderPanel above the primaryCommander gate. Human-verify checkpoint approved. UI-04 closed.
- Code review: 4 warnings found → all auto-fixed (WR-01 removeCard error swallow, WR-02 DeckColumn stale closure, WR-03 invalid aria-pressed on span pips, WR-04 dead GridCell deckCard prop). Re-review clean.
- Schema drift: clean (no schema files changed after migration).
- Phase goal verification: PASSED 11/11 must-haves.
- UI review (retroactive 6-pillar audit): PASS 21/24 with 3 advisory findings (the reason for Phase 03.1).
- Phase 03 marked complete via `phase complete`. STATE.md + ROADMAP.md + REQUIREMENTS.md updated. All artifacts committed.
</completed_work>

<remaining_work>

Phase 03.1 plan creation + execution:
1. `/gsd-plan-phase 03.1` — expect a small plan, likely 1-2 waves, probably 2-3 tasks total.
2. Fix CommanderPanel "Change commander" / "Remove partner" focus rings — add `focus:outline-none focus:ring-2 focus:ring-accent rounded` (src/components/CommanderPanel.tsx:74, 79-84). Match the ViewToggle pattern.
3. Fix DeckColumn `scrollRef` — currently calls `scrollTop = 0` on inner `div` (DeckColumn.tsx:78) but the actual scroll container is the outer `flex-[2]` wrapper in DeckWorkspace.tsx:55 holding `max-h`. Two options: (a) remove `overflow-y-auto` from DeckColumn's root and pass a scroll-reset mechanism to the outer wrapper, or (b) lift the ref to the correct container. Verify with an e2e test that view-toggle at overflow deck sizes actually scrolls to top.
4. Fix CardResultCell focus state — outer `div` has `tabIndex={0}` with no visible focus (src/components/CardResultCell.tsx:60-62). Either add `focus:outline-none focus:ring-2 focus:ring-accent` to outer div, or drop `tabIndex={0}` and rely on inner `(+)` button's tab stop.
5. Rerun UI review — expect full marks on Visuals, Spacing, Experience Design pillars.

Minor (possibly fold in, possibly defer): `mt-0.5` off-scale nudge in CardResultCell overlay; CommanderPanel action-button aria-label enrichment.
</remaining_work>

<decisions_made>

- `oracleid:` is the locked Scryfall prints operator for this project (live probe result).
- CommanderPanel renders unconditionally above the primaryCommander gate (rule: commander search must always be reachable).
- Wave 4 presentational components stay pure — no store imports ever.
- Phase 03.1 was chosen over backlog for the UI review follow-ups: 3 focus-ring / scroll-container issues are small but measurable; running before Phase 4 keeps design contract tight.
</decisions_made>

<blockers>
- None.
</blockers>

## Required Reading (in order)

1. `.planning/phases/03-deck-building-card-display/03-UI-REVIEW.md` — the 6-pillar audit with the exact 3 fix recommendations and file:line refs.
2. `.planning/phases/03-deck-building-card-display/03-UI-SPEC.md` — design contract to match on focus-ring / spacing / interaction decisions.
3. `.planning/ROADMAP.md` Phase 03.1 section — goal + success criteria already customized.
4. `src/components/ViewToggle.tsx` — the focus-ring pattern to mirror in CommanderPanel.
5. `./CLAUDE.md` — project conventions (commit cadence, test coverage expectations, E2E requirement per phase).

## Infrastructure State

- Branch: `main` (no feature branch — `branching_strategy: none`)
- Git working tree: clean
- Dev server: not running at pause
- Last tests: unit 281/281 ✅, e2e 27/27 ✅, typecheck ✅

<context>
The autonomous --only 3 run went smoothly with two human checkpoints (Scryfall probe verdict + end-of-phase eyeball verification), both approved. UI review was the only surprise — not a gate, but flagged 3 small-but-real keyboard/scroll issues worth fixing before moving into Phase 4 (Live Validation). User chose to queue 03.1 rather than backlog them. Context at 89% when pausing — fresh start recommended for the planning round.
</context>

<next_action>
Run `/clear`, then `/gsd-plan-phase 03.1`. The goal + success criteria + 3 specific fix locations are already in ROADMAP.md Phase 03.1 section and 03-UI-REVIEW.md "Top 3 Fixes". The planner has everything it needs.
</next_action>
