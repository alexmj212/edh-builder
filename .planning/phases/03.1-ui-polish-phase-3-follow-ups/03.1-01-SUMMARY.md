---
phase: 03.1-ui-polish-phase-3-follow-ups
plan: 01
subsystem: commander-panel
tags: [ui-polish, a11y, focus-ring, wcag]
requires: []
provides:
  - "CommanderPanel FullCard action buttons (Flip / Change commander / Remove partner) carry focus:outline-none focus:ring-2 focus:ring-accent rounded"
  - "Regression tests asserting focus-ring classes on all three action buttons"
affects:
  - src/components/CommanderPanel.tsx
  - src/components/CommanderPanel.test.tsx
tech_stack:
  added: []
  patterns:
    - "Reused ViewToggle's focus-ring Tailwind pattern (non-inset variant for standalone text buttons: rounded instead of ring-inset)"
key_files:
  created: []
  modified:
    - src/components/CommanderPanel.tsx
    - src/components/CommanderPanel.test.tsx
decisions:
  - "TDD split into test(RED) + fix(GREEN) commits instead of the plan's single atomic commit — tdd=\"true\" flag mandates RED/GREEN commit separation per GSD TDD workflow"
  - "Used npm/vitest (npm run test) instead of plan's `bun test` — project ships with npm scripts and no bun is installed; verification semantics identical"
metrics:
  duration: 3min
  completed: 2026-04-15
---

# Phase 03.1 Plan 01: CommanderPanel Focus Rings Summary

One-liner: Added `focus:outline-none focus:ring-2 focus:ring-accent rounded` to the FullCard Flip / Change commander / Remove partner buttons in `CommanderPanel.tsx`, closing UI-REVIEW Priority Fix #1 (WCAG 2.1 2.4.7 keyboard focus visibility).

## What Shipped

- `src/components/CommanderPanel.tsx` — FullCard sub-component: Flip button (line 73) and action button (line 82) className strings now include `focus:outline-none focus:ring-2 focus:ring-accent rounded`. No other lines changed.
- `src/components/CommanderPanel.test.tsx` — new nested `describe('focus rings (Phase 03.1 UI polish)', ...)` block with 3 tests asserting `focus:outline-none`, `focus:ring-2`, `focus:ring-accent`, and `rounded` classes on Change commander, Remove partner, and Flip buttons.

## Commits

| Hash    | Type | Message                                                      |
| ------- | ---- | ------------------------------------------------------------ |
| 6ab3237 | test | add failing focus-ring regression tests for CommanderPanel   |
| 9e373b8 | fix  | add focus rings to CommanderPanel action buttons             |

## Verification

- `npx vitest run src/components/CommanderPanel.test.tsx` → 14 passed (11 pre-existing + 3 new)
- `npm run test` (full unit suite) → 284 passed / 26 files (baseline 281 + 3 new)
- `npm run typecheck` (`tsc -b`) → exit 0
- `grep -c 'focus:ring-2 focus:ring-accent' src/components/CommanderPanel.tsx` → 2 (Flip + action button)
- `grep -c 'focus:outline-none' src/components/CommanderPanel.tsx` → 2
- `git diff` between tests and fix shows only className string additions on the two FullCard button elements

## Acceptance Criteria

- [x] FullCard Flip button has `focus:outline-none focus:ring-2 focus:ring-accent rounded` in source
- [x] FullCard action button (Change commander / Remove partner) has the same four classes in source
- [x] 3 new regression tests in `CommanderPanel.test.tsx` assert the classes; all pass
- [x] Full unit suite green (no regressions in existing CommanderPanel tests)
- [x] Change committed atomically referencing UI-REVIEW Fix #1

## Deviations from Plan

**1. TDD commit split instead of single atomic commit (plan Task 2)**
- **Reason:** Plan frontmatter sets `tdd="true"` on Task 1 while Task 2 prescribes a single atomic commit; GSD TDD workflow mandates separate RED (failing test) and GREEN (passing fix) commits. Followed TDD protocol.
- **Outcome:** Two commits (`6ab3237` test + `9e373b8` fix) instead of one; cleanly revertable, same net diff.

**2. `npm`/`vitest` instead of `bun`**
- **Reason:** Plan references `bun test` / `bun run test`, but project is npm-based (`scripts.test = "vitest run"`) and `bun` is not installed in this environment.
- **Outcome:** Used `npx vitest run …` and `npm run test`. Equivalent semantics — same vitest runner.

No architectural changes, no Rule-1/Rule-2 auto-fixes were required beyond the planned work.

## Self-Check: PASSED

- FOUND: src/components/CommanderPanel.tsx (modified — focus ring classes present)
- FOUND: src/components/CommanderPanel.test.tsx (modified — 3 new focus-ring tests)
- FOUND commit: 6ab3237 (test — RED)
- FOUND commit: 9e373b8 (fix — GREEN)
